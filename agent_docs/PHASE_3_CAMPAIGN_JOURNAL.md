# PHASE 3 — CAMPAIGN JOURNAL
## Process Telemetry: How the Factory Behaves in the Real World

**Project:** Cyber Pharma v1 · **Campaign:** Phase 3 BIM Campaign (map v1.0)
**Purpose:** record how the PROCESS behaves — not what gets built (retrospectives own that). This journal is the ore for BIM_PLAYBOOK v1.1; it gets harvested at campaign close into a playbook amendment kit. **Maintenance (Director-ratified 2026-08-28):** two-seat, append-only. The Architect (JARVIS) writes entries at each module close + at friction moments; the QA Lead (Sol) appends QA-side lessons after each Gate Q engagement. Entries carry author tags (Architect-logged / QA-logged). Neither seat edits the other's entries — disagreement is written as a new entry, never as an amendment. The canonical copy is the REPO copy; each seat works only from the latest Director-committed version, and the Director is the sole merge point (commit after each seat's update). Director reviews and corrects both seats.
**Standing rule:** every future Factory campaign (Phase 4+, MissionControl backend, Payment Portal, demo portal) keeps its own journal and harvests it at close. The journal is how the factory compounds.

---

## Entry Template (per module)

```
### [MODULE-ID] — closed YYYY-MM-DD
- Playbook prescribed: (what the doctrine said should happen)
- Actually happened: (what did)
- Divergence + why: (none | description; improvised rulings the playbooks lacked)
- Gate Q cycles: N (verdicts received, in order; reasons for any FAIL/BLOCKED)
- Handoff friction: (folder freeze, AC clarity, spec disputes, restarts)
- Coordinator overhead: (git/cloud/admin actions this module demanded)
- Time shape: (sessions consumed; where time actually went vs expectation)
- Keep / Change / Drop: (one line each — the harvest seeds)
```

---

## Entry 0 — Campaign Starting Conditions (2026-08-11)

**Governing doctrine at campaign open:** BIM_PLAYBOOK v1.0 (ACTIVE; pilot-tested on ADK Harness, six modules, rig conditions) · FEAT_PLAYBOOK v1.0 (ACTIVE; unused so far) · BUG_FIX_PLAYBOOK v0.1 + amendment kit (NOT finalized — base file still missing; FIX-001 will run on the amendment kit as active record) · QA_PLAYBOOK + AC-sync patch (patch NOT yet applied to live repo — pre-flight item 2) · SOFTWARE_FACTORY_PLAYBOOK + module-identity insert kit (same status) · Campaign Map v1.0 (this campaign's own invention — the map itself is an untested artifact class).

**What is untested about the doctrine (the campaign's process-risk register):**
1. BIM_PLAYBOOK has never run outside the rig — mothership conditions add: real repo history, a live deployed surface, KIP registry interplay, doc-repo/project-repo split, and a Coordinator juggling three principals.
2. FIX modules have never run under the amendment-kit-only state (no finalized base playbook). FIX-001 is the live test of whether that's workable or clunky.
3. The AC-seed pattern (map pre-seeds ACs → module ACCEPTANCE_SPEC refines) is new — invented in the Campaign Map. Nobody knows if pre-seeded ACs help Sol or fight him.
4. The parallel rig lane (Proto 06 running during BIM-000..001 with a hard transfer-gate on BIM-002) is the first simultaneous rig+mothership operation. Coordination cost unknown.
5. Sol's QA loop has never gated database-shaped deliverables (migrations, policies, seeds) — everything pilot-tested was app-code-shaped. The five-word vocabulary may need interpretation guidance for "the migration replayed clean but the shadow project diff has a naming nit."
6. Two-lab operation (this Architect lab + Engineer terminal + QA lane) at campaign scale — context-transfer overhead between labs is the standing suspect for where time silently goes.

**Pre-registered predictions (so the harvest is honest — graded at close):**
- P1: BIM-001 will overrun its estimate; migration authoring against a 15-table contract will surface at least three DATA_CONTRACT ambiguities requiring Architect rulings mid-module.
- P2: The AC-seed pattern will save net time but at least one module's seeds will need rewriting because the build revealed the seed tested the wrong thing.
- P3: The Proto 06 transfer gate will hold BIM-002 authoring by some days — and it will be worth it (zero policy rework downstream).
- P4: At least one improvised process ruling will be needed that no playbook covers (candidate: how migration files are reviewed at Gate Q — diff-reading vs replay-evidence).
- P5: The journal itself will get skipped at least once under momentum and need backfilling (meta-prediction; if it happens, the v1.1 amendment needs a cheaper capture mechanism).

**Starting facts:** repo `cyber-pharma-dev-v1-phase-3` @ 6f6e63d, triad green at 26/120, DB baseline documented (2 tables / 3 policies), pre-flight checklist items 1–5 open at time of writing.

---

## Module Entries

### BIM-000-CYBER-PHARMA — closed 2026-08-14 (Gate Q PASS; final close-out batch committed 2026-08-27 post-Coordinator-absence)

- **Playbook prescribed:** author FINAL from recon → Coordinator scope approval → Engineer ONE-message Plan Mode → plan-QA → build → spec finalized with evidence → Coordinator commits per concern → Gate Q against committed SHA → verdict → close.
- **Actually happened:** prescribed path held end-to-end with ONE structural improvisation: Gate Q split into **PRE-Q on the working tree, then certification** (Director ruling — QA/rework before one final commit). Engineer surfaced 4 plan-mode flags instead of resolving; all ruled by Architect/Coordinator. Spec finalized with evidence; Sol built a 23-case plan with declared gaps and ran it with the Director as hands. Verdict: **PASS, zero rework** — first-attempt clean.
- **Divergence + why:** (1) PRE-Q invention — playbook's commit-then-QA sequence fought the Director's preference for a single clean close commit; ruled live, worked well, v1.1 candidate. (2) Branch drift — spec said `bim-000-cyber-pharma`, disk said `phase-3-1`; disk won; single-branch small-module pattern needs a playbook sentence. (3) QA intake was assembled ad hoc (spec + handoff + baseline + missing catalog paste) — the intake-checklist gap. (4) Coordinator hospitalization froze the machine one commit from close for ~13 days; the freeze was CLEAN — recap + RECOVERY + journal rebuilt full context in one session. Disaster drill passed by accident.
- **Gate Q cycles:** 1 (PASS). Within the cycle: one QA self-correction (contract-overreach on phase2.md, ruled back to contract-strictness) and one evidence substitution (missing 08-11 catalog paste → fresh read-only catalog, documented not hidden).
- **Handoff friction:** spec-lifecycle ambiguity (seeded vs finalized perceived as two artifacts — banner fix queued); stale-log/evidence-filename confusion; runner cwd false-failures. All logged with candidate rules.
- **Coordinator overhead:** 7 per-concern commits + close batch; Stripe key purge/rotation; phase2.md sibling recovery; journal staging (missed pre-launch — P5); PRE-Q adjudications; Gate Q terminal execution as Sol's hands.
- **Time shape:** authoring <1 session · plan+build 1 session (2026-08-13) · PRE-Q+Gate Q ~1 day (08-13→14) · close-commit latency 13 days (external, hospitalization — not process cost). Est. "light (one session)" held for build; QA end-to-end roughly doubled wall time — expected for the ritual's maiden run.
- **Keep:** AC-seed pattern (Sol consumed the seeded ACs directly into traceability — zero friction; P2 partially unsupported so far) · plan-mode flag culture (caught a defective instrument pre-build) · PRE-Q · attestation-vs-observation discipline in QA evidence.
- **Change:** journal staging = campaign-launch prerequisite · QA_INTAKE_CHECKLIST standard · Coordinator source evidence preserved inside the module package · spec lifecycle banner · unique evidence filenames · repo-root fail-closed runners · boundary-aware predicates.
- **Drop:** nothing yet — no prescribed step proved dead weight on a light module; re-evaluate on BIM-001's heavier run.
- **Prediction scoreboard after module 1:** P4 CONFIRMED (multiple improvised rulings: PRE-Q, contract-strictness, evidence substitution) · P5 CONFIRMED (staging failure, self-caught) · P2 leaning against (seeds held) · P1/P3 pending (BIM-001/002 not yet run).
- **New org proposal surfaced (route to Coordinator decision before BIM-001):** "Cody" — dedicated QA execution agent for deterministic scripting/collection; Sol retains adjudication; Director retains git. Origin: runner-cwd + evidence-log pain.

---

### FIX-001-CYBER-PHARMA — closed 2026-08-27 (Gate Q PASS, zero rework)

- **Playbook prescribed:** FIX grammar per amendment-kit-only doctrine (first live test of that state): diagnosis-led manager, seeded spec, plan mode, build, Gate Q.
- **Actually happened:** ritual held; TWO plan-mode flags (logout action-vs-state-read; single cross-tab listener needing one new file) → both ratified as spec refinements at plan time, zero build cost. Build clean; Gate Q PASS first attempt. KIP-2 reproduction attacked live across 3 identities × 2 viewports × hard-refresh + stale-localStorage + tampered-role + client-nav paths — all held.
- **Divergence + why:** (1) **One-Walk Rule invented (Director ruling, mid-module):** manual eyes-on executes ONCE — QA Lead designs/directs, Director executes as hands, evidence serves both PRE-Q and certification. Replaces the double-walk implied by BIM-000's PRE-Q + Gate Q sequence; independence relocated to test-design + adjudication, where it always really lived. Major v1.1 amendment. (2) G1 seed wording refined (P2 evidence: seed tested "zero store refs" when the contract meant "zero state reads"). (3) One new file (NavAuthRefresh) ratified beyond the manager's literal list via flag-don't-deviate.
- **Gate Q cycles:** 1 (PASS). One out-of-contract exploratory finding (QA-FINDING-001, dark-mode login contrast) correctly classified non-blocking and routed to the findings ledger — the in-scope/out-of-scope boundary held under live conditions.
- **Handoff friction:** Engineering handoff sequenced commits-before-walk vs PRE-Q's walk-first — resolved by One-Walk; playbook needs the canonical order written. Terminal ghost (EADDRINUSE stale server) cost minutes — environment-hygiene line for the walk script template.
- **Director overhead:** 3 per-concern commits + push; SHA pin; walk execution (~20 min under Sol direction); two plan-mode adjudications.
- **Time shape:** authoring + plan + build + Gate Q inside ONE day (2026-08-27) — the amendment-kit FIX grammar is workable; clunkiness feared in risk-register item 2 did not materialize.
- **Keep:** One-Walk Rule · plan-mode flag culture (2-for-2 modules now) · ratified-refinement path for seed wording.
- **Change:** canonical walk/commit order into the playbook · "Director" rename propagates to all templates (Coordinator retired 2026-08-27) · walk scripts open with environment hygiene (kill stale servers, production mode only).
- **Drop:** the double-walk implication in the BIM-000-era PRE-Q description — superseded by One-Walk.
- **Prediction scoreboard:** P2 now CONFIRMED (G1 seed refinement) · P4 re-confirmed (One-Walk improvisation) · P5 previously confirmed · P1/P3 pending BIM-001/002.
- **Routing out:** QA-FINDING-001 → findings ledger (rides next UI-scoped module or BIM-005 polish). Cody ruling still parked as BIM-001 pre-flight. **Proto 06 launch is now URGENT-parallel: BIM-002 hard-gates on its transfers.**

---

### BIM-001-CYBER-PHARMA — closed 2026-08-31 (Gate Q PASS, zero rework; certified SHA `9f8c80d`)

- **Playbook prescribed:** author FINAL from contract → Director pre-flight rulings → ONE-message Plan Mode → build → spec evidence-filled → walk-first PRE-Q → per-concern commits → SHA pin → certification → close-out.
- **Actually happened:** prescribed path held. Pre-flight produced two rulings (Cody → QA seat; Gap-6 + accounts spine). Plan Mode surfaced THREE blocking flags (authority docs off-disk / toolchain absent / role-vocabulary conflict), all ruled same-day. X0 fired STOP on a phantom function (ruled: chain creates it). Chain of 15 migrations + 3 runners authored and all engineering gates X0–X7 green **inside one session day (08-28)**. PRE-Q executed by Cody (QA's execution agent, first live run) 08-31. Gate Q returned **HOLD** — not for a defect but because Cody was shut down before independent AC14/AC15 regression evidence was returned; Director ran the board himself against the close specimen; Sol flipped to **PASS** same day. Close-out batch wording-only.
- **Divergence + why:** (1) **Five contract-side defects, zero build defects:** accounts tier missing from the 15-table contract · `update_updated_at()` claimed by a LOCKED contract but never deployed · `report_files` columns never enumerated in the extraction · AC3 pre-chain function count stale (ERRATUM-Q1) · AC12 "all sixteen" contradicted manager law (ERRATUM-Q2). The two errata were the Architect's own spec prose. (2) **Disk overruled a LOCKED doc** (DATA_CONTRACT §3) — hierarchy worked. (3) **Credential-boundary pattern** improvised twice (dashboard paste X0; Director login→gen→logout X6). (4) **Cody released too early** → HOLD cycle that cost a few hours, no rework. (5) QA ran on its own disposable branch `qa/bim-001-cody-01`, discarded unmerged, evidence retained — a new lane pattern, now precedent (and adopted for Proto 06 by Director ruling 08-31).
- **Gate Q cycles:** 1 (HOLD → PASS within the cycle; HOLD reason = missing independent regression evidence, not a finding). Errata: 2, both spec wording. Rework: 0.
- **Handoff friction:** Engineer's handoff again sequenced commits-before-QA (2nd module running — canonical walk-first order is now a REQUIRED v1.1 line) · dashboard SQL editor one-grid-per-run · IPv6-only direct hosts (use session pooler — documented for QA) · `information_schema` vs `pg_catalog` on Supabase (instrument lesson #3) · spec wording not re-read against erratum findings at engineering close (Claudy's own retro lesson).
- **Director overhead:** authority-folder staging · two throwaway Supabase projects · X0 dashboard catalog (twice, due to the grid quirk) · X6 types generation in own terminal · One-Walk (AC13) · independent regression board (AC14/15) · 4 per-concern commits + close commit · SHA pin · pending: LIVE apply, DATA_CONTRACT amendment staging, credential rotation.
- **Time shape:** pre-flight rulings + authoring (08-28 AM) → plan, 3 flags, X0, full chain, X0–X7 green, handoff (08-28, ONE day) → PRE-Q/Cody + HOLD + regression + PASS + close-out (08-31). ~2 working days elapsed for the heaviest module so far. P1's predicted TIME overrun did not occur; the overrun was in contract clarity, all caught pre-build.
- **Keep:** pre-flight ruling session (caught a whole missing table for the price of a conversation) · plan-mode flag culture (3-for-3) · X0 STOP law · authority-folder-with-precedence-README pattern · assert-then-create migrations · Director-as-hands for anything credentialed · execution/adjudication split on the QA side · disposable QA/rig branches discarded unmerged.
- **Change:** write the canonical walk-first order into BIM_PLAYBOOK (REQUIRED) · execute DOCTRINE_SYNC before the next module (FLAG-A must not recur) · **release the execution agent only on the QA Lead's evidence-complete signal** · Engineer re-reads spec prose against errata before handoff · hands-pattern catalog scripts one-section-per-run · spec authoring rule: never state counts that the manager's own laws contradict.
- **Drop:** the "no rig code in the mothership repo" absolute in the Proto 06 brief — superseded by the disposable-branch-never-merged pattern with fences (fresh throwaway DB, copy-not-merge transfers, separate session).
- **Prediction scoreboard after module 3:** **P1 CONFIRMED** (≥3 contract ambiguities; 5 counted) · P2 CONFIRMED · P4 CONFIRMED (credential boundary, HOLD-for-evidence, branch pattern — all un-playbooked) · P5 CONFIRMED · **P3 pending** (Proto 06 not yet run; BIM-002 not yet authored — graded next).
- **Routing out:** `report_files` fidelity flag → BIM-004/005 · DATA_CONTRACT §3 amendment → Director doc-repo staging · LIVE apply → Director · throwaway creds → rotate · Proto 06 → disposable branch `proto-06-rig`, serial, no QA, transfers by copy → then doctrine sync → then BIM-002.

### PROTO-06-RLS-HARNESS — closed 2026-08-31 (R1–R5 green, TRANSFERS delivered; rig lane, no Gate Q by design)

- **Playbook prescribed:** rig lane per PROTO_06_RIG_LAUNCH_BRIEF (08-28) + PROTO_PLAN_06 v1.0 (07-13): separate rig repo, five-table miniature, policies T-1..T-6 red-green, attack battery, one-command reproof, TRANSFERS.md as gate currency. Serial, no QA seat.
- **Actually happened:** Director ruled 08-31 AM the rig runs INSIDE the mothership repo on a disposable never-merged branch (superseding the brief's rig-repo absolute); branch cut as `phase-3-proto-6` (ruling said `proto-06-rig` — disk won, logged). Launch BLOCKED on first Claudy message: brief + plan absent from disk (lived only in the Architect stack — Part 2 lab and the July Fable session), and API keys cannot execute DDL. Architect recovered both documents from the lab record, reconciled the July plan against Gap-6/R-2/branch ruling as v1.1 (header supersedes body), delivered same session; Director staged at `agent_docs/ACTIONS/PROTO06/` (disk path won over the launch order's `PROTO/06/`) and added `PROTO06_DB_URL` (session pooler, `aws-1` host — launch order said `aws-0`, wrong). R1→R5 then ran in ONE session: 8 policies (6 planned + helper pair + one Director-approved addition), 80-cell matrix, 32-case attack battery, full from-scratch reproof twice identical, TRANSFERS.md + FINDINGS.md written. Transfers COPIED to `main` via `git checkout <branch> -- <paths>`; rig scaffolding (migrations/scripts/evidence) stays on the disposable branch.
- **Divergence + why:** (1) **FINDING-1 / 8th policy:** T-3 role-gated UPDATE landed correct and affected 0 rows — Postgres reads UPDATE/DELETE target rows under SELECT semantics, and `businesses` had no SELECT policy. Silent no-op indistinguishable from a working deny. Engineer held RED and flagged rather than build; Architect ruled APPROVED (T-1 pattern on `businesses`, different operation, one-per-op law intact). **First-class transfer: SELECT before any write policy on every tenant table.** (2) **Storage leg DEFERRED to Proto 01** (Director ruling): BIM-002 does not consume Storage isolation; recorded as N-1 owed, not dropped. (3) **Browser-client leg deferred** by the in-repo branch fence (no `src/` from the rig lane); harness exercises the same supabase-js publishable-key auth path; SSR/cookie handling recorded as N-2 owed. (4) **Plan v1.1 reconciliation** — the July plan predated Gap-6 (`user_roles` table, super-admin seed identity) and R-2 (accounts spine); reconciled by header rather than rewrite so the technical body stayed intact. (5) Rig ran R2 (six policies) and R4+R5 as single authorizations rather than per-policy stops — serial lane with no QA seat made per-step stops pure overhead.
- **Gate Q cycles:** n/a (rig lane). Rig gates R1–R5: 5/5 green first attempt. One flag-held finding (F-1), zero rework.
- **Handoff friction:** launch inputs not on disk (documents AND credential) — **fourth doc-staging miss of the campaign** (BIM-000 journal, BIM-001 authority folder, PROTO06 docs, PROTO06_DB_URL). Wrong pooler generation in the launch order (caught by recon against BIM-001 evidence). Engineer correctly refused to edit `.env.local` (BIM-000 R2). Verification-instrument defect #3 of the campaign (`array_agg` over the pg driver). Director asked for one-step-at-a-time git guidance mid-close — lectures failed, single steps worked.
- **Director overhead:** branch cut · document staging · `.env.local` line · five Claudy Prompts (launch, unblock, R1 approve, R2, FINDING-1+R3, R4+R5) · checkpoint commit on the disposable branch · transfer copy-forward + two commits on `main` · FINDINGS.md relocation.
- **Time shape:** blocked 11:36 → unblocked ~12:30 → R1 board ~13:00 → R2/R3 mid-afternoon → R4/R5 + TRANSFERS ~18:30 → copied to `main` ~19:10. **One calendar day, launch to consumed transfers,** including a recon-blocked first hour.
- **Keep:** disposable never-merged branch with fences (fresh DB, copy-not-merge, separate session) · flag-don't-build on unplanned policies · red-then-green as the standard for every policy landing · one-permissive-policy-per-op enforced mechanically · self-cleaning probes (destroy only what you created) · ground-truth verification after mutation attacks · reconcile-by-header when a plan predates rulings.
- **Change:** launch-line prerequisite becomes a mechanical check: documents on disk + credentials in env BEFORE the runner's first message (REQUIRED v1.1 line — four misses is a pattern, not bad luck) · pooler host recorded in the module manager, never recalled · BIM-002 manager ships policy landing order (helpers → SELECT → writes) as law, not guidance · BIM-002 pre-flight must rule the `accounts` access pattern (N-6) and the F-4 A/B (`IN (subquery)` vs helper) at realistic volume · rotate throwaway credentials at lane close (both BIM-001 throwaways + the Proto 06 project password, which transited chat).
- **Drop:** the brief's "rig repo" runner clause and the July plan's `user_roles`/super-admin seed — both superseded, both preserved as documented supersession rather than deleted.
- **Prediction scoreboard after Proto 06:** **P3 SUPPORTED** (gate held BIM-002 zero days of Director time beyond one session; F-1 alone justifies the lane; final grade CONFIRMED only if BIM-002 passes Gate Q with zero policy rework) · P1/P2/P4/P5 unchanged (P4 re-confirmed: in-repo branch ruling, FINDING-1 ruling, Storage deferral — all un-playbooked).
- **Routing out:** TRANSFERS.md consumed by the Architect 08-31 → BIM-002 authoring UNBLOCKED pending DOCTRINE_SYNC · N-1 Storage → Proto 01 · N-2 browser/SSR leg → later UI lane · N-3 live-session revocation → BIM-002 or CRV · N-6 accounts pattern → BIM-002 pre-flight ruling · throwaway credential rotation → Director, at BIM-002 harness re-point close · `proto-06/` landing zone on `main` → BIM-002 manager assigns permanent homes for harness + policy templates.

**Director rulings logged this session (not module-specific):**
- **LIVE APPLY DEFERRED to campaign close** (Director, 08-31): the dev backend stays at its 2-table baseline through the BIM campaign; each module certifies on the clean throwaway; all migration chains apply to the dev backend in ONE deliberate session at Phase 3 close, in order, with a catalog check after. Rationale: focus — throwaway/red-team momentum is the campaign's shape, and mid-stream live touches risk conceptual mix-up. Consequence: Sol's per-module gate is unchanged; a "Phase 3 apply session" becomes a named close-out step in the campaign map (patch at DOCTRINE_SYNC).
- **"Claudy Prompt" / "Sol Prompt" / "Cody Prompt"** is the sole term for anything pasted to an agent (Director, 08-31). Previous variants retired.
- **Architect voice:** human-facing output notched down on Director request (08-31); agent-facing prompts stay Precision Mode per CYBERIZE_PROMPT_ENGINEERING_MANUAL v1.1 (seven-element coverage; evaluation lens + iteration permission now stated explicitly). "Notch" is the reset word.

## Friction Log

### 2026-08-13 — BIM-000 Plan Mode (pre-build; Architect-logged)

- **Engineer FLAG-1 — journal not staged:** manager cited the journal as "live"; it wasn't on disk in the project repo (pre-flight item 1 unexecuted). First P5 signal, later confirmed at PRE-Q (see below).
- **Engineer FLAG-2 — predicate artifact discovered at plan time:** the numbered-color grep's 5 hits are `translate-`/`slide-` substrings + the banning comment itself; real violations 0. Ruled: gate stands frozen for comparability this module; instrument rebuilt at campaign level. First live P4 evidence — caught before a single build line, by the plan-mode verification ritual.

### 2026-08-13–14 — BIM-000 PRE-Q / first live Gate Q friction

- **Acceptance Spec lifecycle ambiguity:** seeded spec vs Engineer-evidence-filled spec was initially perceived as two artifacts. Candidate fix: lifecycle banner `SEEDED → ENGINEER EVIDENCE-FILLED → QA-VERIFIED`.
- **QA intake incompleteness:** Gate Q needed finalized spec + handoff + DB baseline + Coordinator-owned source evidence. Candidate fix: standard `QA_INTAKE_CHECKLIST`.
- **Branch-name drift:** Acceptance Spec named `bim-000-cyber-pharma`; disk/handoff branch was `phase-3-1`. Disk truth won; document drift logged.
- **PRE-Q ruling:** Director prefers QA/rework before one final commit. Pilot split into PRE-Q on working tree, then official Gate Q certification against committed SHA.
- **Contract overreach caught:** QA began byte-comparing recovered `phase2.md` even though P3 required only recovery into `agent_docs/`. Ruling: adversarial QA must be strict to the contract, not silently stricter.
- **Historical evidence retention gap:** original 2026-08-11 DB catalog paste was unavailable at Gate Q. Fresh read-only catalog matched baseline, but historical byte-faithful comparison was impossible. Candidate fix: preserve Coordinator-owned source captures inside the module evidence package.
- **Verification-instrument defect:** frozen numbered-color grep still returns 5 artifacts while true violations are 0. Keep frozen predicate for comparability; design corrected boundary-aware instrument for future modules.
- **QA runner cwd defect:** first scripted runner assumed repo-root execution and produced false failures when launched from `scripts/`. Candidate rule: resolve repo root, print runner version/root, fail closed on missing anchors.
- **Predicate semantic-scope defect:** generic greps counted test fixtures/comments as production violations. Candidate rule: QA instruments must encode semantic scope, not just keywords.
- **Evidence-log confusion:** generic log names caused a stale invalid run to be pasted as current evidence. Candidate rule: unique test-specific evidence filenames.
- **Execution-agent opportunity:** deterministic QA scripting/collection should move to a dedicated QA execution agent ("Cody"), while Sol retains adjudication and the Director retains commit/push authority.
- **Pre-commit cleanup handoff:** after QA, Engineering/QA execution agent performs bounded cleanup + regression and returns a clean working-tree report; Director performs git writes manually.
- **Campaign-journal staging failure:** the canonical journal existed in the Architect workspace but was never staged into the repo, validating prediction P5. Candidate rule: journal presence is a campaign-launch prerequisite, not a module-close memory task.

### 2026-08-28 — BIM-001 pre-flight (Part 2 lab, first session; Architect-logged)

- **Lab handoff Part 1 → Part 2 executed clean:** handoff doc + this journal rebuilt full context in one session; Director confirmed FIX-001 close-out committed. Second successful cold-start recovery (first: BIM-000 hospitalization freeze). The handoff-doc pattern is 2-for-2.
- **Director recall check on schema authority (healthy):** Director queried whether the legacy demo DB had been adopted as the schema. Ruling re-confirmed from LEGACY_DEMO_FORMAT_MAP v1.0: demo = format truth + seed source, NEVER schema authority; FRANK_API/Triangulation fifteen-table contract stands. No doctrine change — logged because the question itself validates keeping the ruling written where it can be re-cited in one search.
- **Cody ruling RESOLVED (Director, 2026-08-28):** routed OFF the engineering desk entirely — QA seat staffing is QA's internal decision (Sol's shop); engineering carries zero dependency on any QA execution agent. Director's framing: "our job is to get the engineering done — no QA." Parked item closed; recorded as manager R-1. Org-boundary clarification is itself a v1.1 candidate line: *execution-side proposals originating in QA friction route to the QA seat, not to Director pre-flight.*
- **Gap-6 ratified WITH material scope discovery — first P1 evidence, pre-build:** Director's org ruling (no superadmin in OwedBook; two junction roles `admin`/`member`, alterable-permissions v2 seam; one subscription per owner across all stores) + Coach's account-hierarchy diagram exposed a missing tier in the fifteen-table contract: no organization/group entity above `businesses`. **Ruling: sixteenth table `accounts` added** (owner spine; `businesses.account_id`; `subscriptions.account_id` NOT business_id). This is a DATA_CONTRACT ambiguity requiring an Architect/Director ruling mid-module-authoring — P1 predicted ≥3 during BIM-001; scoreboard now 1, and it surfaced BEFORE build rather than during it (plan-mode-era discovery is cheaper than build-era — harvest note for whether pre-flight ruling sessions should be a named stage).
- **Frank-front telemetry:** five-rulings escalation sent via Coach (Director-executed); golden-dataset ask re-issued with the BIN+PCN+group rider + desktop computed-output columns; cloud-access explicitly decoupled from the dataset ask (file drop now, GCS migration later) to keep the parity dependency light.
- **BIM-001-CYBER-PHARMA authored and delivered** (manager + seeded spec AC1–AC15 + QA/ born in, zip per delivery doctrine). Awaiting Director review → Claudy launch. Proto 06 rig launch brief queued as next Architect artifact (URGENT-parallel standing).
- **Journal maintenance rule amended (Director-ratified, same session):** two-seat append-only maintenance adopted — Sol appends QA-logged entries after each Gate Q; author tags mandatory; repo copy canonical; Director sole merge point. Rationale: QA-side friction has been the densest ore in this journal but was captured secondhand; firsthand QA entries improve harvest quality. v1.1 candidate: journal-maintenance protocol as a named doctrine section.

### 2026-08-28 — BIM-001 plan → build → engineering handoff, one day (Architect-logged; module OPEN — close entry follows Gate Q)

- **Time shape (provisional):** authoring, plan mode, three blocking flags, X0, full chain (15 migrations + 3 runners), all engineering gates X0–X7 green, spec evidence-filled — inside ONE session day. The heavy-module overrun P1 predicted has (so far) not materialized as TIME; it materialized as CONTRACT AMBIGUITY — see below.
- **P1 CONFIRMED (3 of ≥3 contract ambiguities requiring rulings):** (1) accounts-tier gap (pre-flight, 08-28 AM — logged above); (2) **X0 discrepancy:** `update_updated_at()` claimed by DATA_CONTRACT §3 (LOCKED) + manager Verified Ground, absent on live — claim inherited from starter-kit-v2 docs, never deployed; ruled option-(i) chain-creates-idempotently + ERRATUM + contract amendment. **Disk overruled a LOCKED doc — the hierarchy (disk > docs > memory) worked exactly as written**; (3) **report_files columns never enumerated in the FRANK_API extraction** — Engineer authored minimal attested shape + fidelity flag rather than inventing columns. All three P1 hits are CONTRACT defects, zero are build defects — harvest headline: this factory's risk lives in contracts, not construction.
- **Plan-mode flag culture 3-for-3 modules:** three blocking flags (authority docs not on disk / toolchain absent / role-vocabulary conflict), all surfaced pre-build, all ruled same-day. FLAG-A is the **doctrine-sync debt biting live** (Entry-0 risk 1, doc-repo/project-repo split): schema authority + campaign map existed only in the Architect stack. Resolved via a staged `authority/` folder with a precedence README. **Standing consequence ruled: execute the DOCTRINE_SYNC_MANIFEST before BIM-002 authoring inherits the same flag.**
- **Credential-boundary pattern hardened (2 rulings, same shape):** X0 live catalog = Director-as-hands dashboard paste (no live URL to Engineer); X6 types generation = Director runs login→gen→logout in HIS terminal (account-wide token never enters the Engineer session; only throwaway-project URLs transited chat, flagged for rotation post-QA). v1.1 candidate: *the Engineer session never holds credentials scoped wider than throwaway.*
- **Instrument lesson #3 (boundary-aware predicates, campaign tally):** prefer `pg_catalog` over `information_schema` on Supabase — third predicate-class defect after BIM-000's grep artifacts and cwd runner. The corrected-instrument rebuild (FLAG-2, campaign-level) keeps accruing evidence.
- **Director-as-hands friction:** Supabase dashboard SQL editor silently returns only the LAST query's grid — X0's 7-section catalog came back 1-of-7 on first pass; re-run one-section-at-a-time. Candidate rule: hands-pattern catalog scripts are authored one-section-per-run or as a single unioned grid.
- **Ordering law re-applied at handoff:** Engineer's handoff again sequenced commits-before-QA; corrected to FIX-001's walk-first order (PRE-Q on working tree → One-Walk → commits → SHA pin → certification). Second occurrence of the same drift — the canonical order STILL is not written into the playbook; escalating from "change" candidate to REQUIRED v1.1 line.
- **Riding to Gate Q:** first database-shaped QA engagement ever (Entry-0 risk 5 live test) · report_files fidelity flag · replay surfaces preserved on both throwaway projects (region/pooler notes handed to QA) · `.temp` gitignore ruling folded into Concern-2 commit.

### 2026-08-31 — BIM-001 PRE-Q execution complete; verdict pending (Architect-logged)

- **Cody ADOPTED by the QA seat** (resolving the BIM-000 org proposal exactly as the Director routed it — QA's call, not engineering's): a Codex-based execution agent ran the deterministic QA suite (`QA/cody-bim001-db-runner.mjs`, `QA/cody-regression-runner.mjs`, report `QA/CODY_QA_EXECUTION_REPORT.md`). First run: 2m46s, AC13 One-Walk recorded, AC14/AC15 re-verified, "no defects observed", zero credentials persisted, zero git. **Gate Q adjudication remains Sol's** — the execution/adjudication split held on its first live outing. Four-seat QA-side topology (Sol adjudicates, Cody executes, Director as hands, Architect absent) is now a live pattern; v1.1 harvest: name it.
- Board still UNCOMMITTED per walk-first order; Director commit batch follows Sol's PRE-Q verdict. Module close entry deferred to verdict. Wall-clock note: engineering handoff 08-28 17:01 → PRE-Q execution complete 08-31 09:40 (weekend inclusive).

### 2026-08-31 — BIM-001 close-out batch shipped WITHOUT the journal (Part 3 lab, first session; Architect-logged)

- **P5 third occurrence:** the 08-31 close-out batch (spec QA-VERIFIED, manager CLOSED, Sol FINAL_PASS) was committed by the Director, but the journal — carrying the BIM-001 module-close entry — was not in the batch. The repo copy (canonical per the 08-28 maintenance ruling) was therefore stale at Part 3 open; Sol's QA-logged entry was blocked behind it, since each seat works only from the latest Director-committed version.
- **Why it slipped:** the journal lived in the Architect workspace and the close-out batch was assembled by the Engineer from the module folder. Nothing in the close-out checklist names the journal as a required file, so momentum dropped it — exactly the mechanism P5 predicted.
- **Ruling (Architect, pending Director ratification by commit):** journal committed as a standalone commit before Proto 06 launch. **v1.1 REQUIRED line (escalated from candidate):** (a) the close-out batch file list MUST include the journal with the module-close entry; (b) the QA Lead's QA-logged entry is a Gate Q deliverable returned with the FINAL_PASS report, not a post-close memory task; (c) the Architect delivers the journal delta to the Director in the same message as the close-out batch, never in a separate lab-only artifact.
- **Harvest note:** three P5 hits across three modules with three different slip points (launch staging, verdict-day momentum, close-out batch assembly). The cheaper capture mechanism Entry 0 asked for is not a reminder — it is putting the journal on the same file list as the artifacts that already get committed.

---

## Harvest (at campaign close)

*(graded predictions P1–P5 · Keep/Change/Drop rollup · the BIM_PLAYBOOK v1.1 amendment kit draft)*
