# RRM-001 RECON AUGMENT — 2026-09-18

**Mode:** Precision, read-only. **Skill:** `_SKILLS/stark-recon-skill-v1.1` (doctrine: disk wins).
**Labels:** VERIFIED / NOT FOUND / CHANGED / NEEDS DIRECTOR. No severity opinions, no proposed fixes.
**Constraints honoured:** no git mutation · no npm install/upgrade · no live Supabase call · no env values printed (key names only).
**Writes made:** this report, the session log, RECOVERY.md header. Build outputs went to `.next/` only (gitignored, `.gitignore:12`).

**Operator overrides noted (skill §Override):** full report pasted on screen per the mission, not the skill's short summary. Report filed in `agent_docs/RECON/` (moved from `agent_docs/RESPONSES/` on the Operator's instruction, 2026-09-18).

## REVISION 2 (2026-09-18, after the Director supplied the source documents)

The Director dropped six files into `agent_docs/`: `FABLE_CODE_REVIEW.md`, `ASTRA_CODE_REVIEW.md`, `PHASE_2.1/{UI_SPEC,DATA_CONTRACT}.md`, `PHASE_2.2/{UI_SPEC,DATA_CONTRACT}.md`. With them: the Finding-ID mapping is now filled (closing table), R8 is re-answered, and four claims the mission had not listed (F4, F8, F10's Report button, A-006) were checked — see **R2-ADDENDUM**. Deploy facts (R9) are **waived by the Director** ("no worries about deployments").

## THREE THINGS THE DIRECTOR MUST SEE FIRST

1. **The commit question is answered — it is a different repository.** Fable reviewed repo `cyber-pharma-dev-v1`, branch `fable-code-review-13sep2026`, pinned SHA `f1113177…` (`FABLE_CODE_REVIEW.md:4,20`). This working copy is `cyber-pharma-dev-v1-phase-3`; that object does not exist here. Astra reviewed a Git-free export and itself states "No claim that this export matches it" (`ASTRA_CODE_REVIEW.md` §2). **Review equivalence remains unproven at commit level**; every claim below was therefore re-checked against current disk.
2. **The dropped Phase-2.1 specs are v1.0 — older than the versions the code and Astra cite.** `PHASE_2.1/UI_SPEC.md:153` and `PHASE_2.1/DATA_CONTRACT.md:154` are both "1.0 | 2026-06-09". Code comments cite UI_SPEC **v1.3 / v1.4**; Astra cites `UI_SPEC.md:237–242`, but the on-disk file has only 153 lines. Consequence: the a11y requirement IS found (v1.0 `:132-134`), but the "pbm may be null / renders as em dash" statement is **not** in v1.0 — there `pbm: string` is non-nullable (`PHASE_2.1/DATA_CONTRACT.md:44`). See R8.
3. **Three Fable findings no longer hold on current disk** (F5, F7 — both closed by FIX-001; F12's "identical copy" is now one comment line apart) and F9's advisory count has moved (8 → 6). Details in the closing table.

---

## R1 BASELINE IDENTITY

| Check | Result | Label |
| --- | --- | --- |
| Branch | `phase-3-rrm-ffm` | VERIFIED |
| HEAD | `5f45fb3db7ed0aa7d38dc6802c3a877c3f119dd9` (== `main`, zero commits ahead) | VERIFIED |
| `git status --porcelain` | **NOT clean** — one untracked file: `?? agent_docs/SESSIONS/session_2026-09-18.md`. That file was created by me at session start today (protocol Step 0). No tracked file is modified. | CHANGED (expected "clean") |
| `merge-base --is-ancestor f1113177… HEAD` | **No.** `fatal: Not a valid commit name` (exit 128); `git cat-file -t` also fails — the object does not exist in this repo. **Review equivalence is unproven at commit level.** | NOT FOUND — Revision 2: the SHA belongs to sibling repo `cyber-pharma-dev-v1` (`FABLE_CODE_REVIEW.md:4,20`) |
| Map cut `6f6e63d` | `11aug2026 - audit passed, build tested, ready for phase 3` | VERIFIED |
| Merges (`--merges -5`) | Exactly one merge commit in the last-5 query: `f7a1d4c 16sep2026 - merge qa/phase-3-bim003 - BIM-003 CLOSED (Gate Q PASS @ c45949e)` — on this branch. | VERIFIED |
| RRM-* under `agent_docs/ACTIONS/` | None. Folders present: BIM-000…003, FIX-001, PROTO06, README.md | NOT FOUND |
| rrm branches | `phase-3-rrm-ffm` (local, current) + `remotes/origin/phase-3-rrm-ffm` | VERIFIED |
| Folder spelling | `agent_docs/ACTIONS` (plural). No `ACTION` folder exists. | VERIFIED |

**`git diff --stat 6f6e63d..HEAD -- src/`** — 9 files, +1361 / −85:

| File | Expected? | Note |
| --- | --- | --- |
| `src/app/(public)/layout.tsx` (56) | expected | |
| `src/components/global/NavbarHome.tsx` (21) | expected | |
| `src/components/global/MobileNav.tsx` (41) | expected | |
| `src/components/global/UserMenu.tsx` (37) | expected | |
| `src/types/supabase.ts` (+1143) | expected | generated types |
| `src/components/global/NavAuthRefresh.tsx` (+28, new) | **FLAG — not in expected list** | FIX-001 cross-tab listener per `KIP_REGISTRY.md:58` |
| `src/__tests__/global/MobileNav.test.tsx` (+60, new) | **FLAG — not in expected list** | see R7 |
| `src/__tests__/global/UserMenu.test.tsx` (+58, new) | **FLAG — not in expected list** | see R7 |
| `src/instrumentation.ts` (2) | **FLAG — not in expected list** | one comment line only: `.env.local.example` → `.env.example` (`src/instrumentation.ts:5`). No code change. |

---

## R2 REVIEW-CLAIM RE-VERIFICATION (current disk)

### `src/app/moose-portal/users/actions.ts`

The file imports only `revalidatePath` and `createAdminClient` (`actions.ts:3-4`). There is no import of `protectPage`, no session read, no role check, and no `NEXT_PUBLIC_ENABLE_MOOSE_PORTAL` read anywhere in the file.

| Function | `createAdminClient()` at | Anything before it (protectPage / session / role / flag)? | Label |
| --- | --- | --- | --- |
| `getUsers` | `:27` (first statement) | None | VERIFIED |
| `getUserById` | `:92` (first statement) | None | VERIFIED |
| `editUser` | `:126` (first statement) | None | VERIFIED |
| `deleteUser` | `:158` (first statement) | None | VERIFIED |
| `addMember` | `:178` (first statement) | None | VERIFIED |

- **`deleteUser` target-role restriction:** none in code. `:160` calls `adminClient.auth.admin.deleteUser(userId)` directly on the supplied id. The comment at `:155` says "Only for member users, not admins" — no statement enforces it. **VERIFIED.**
- **Where the gates do live:** `src/app/moose-portal/layout.tsx:15` (`NEXT_PUBLIC_ENABLE_MOOSE_PORTAL !== "true"` → `notFound()`) and `:17` (`protectPage([AppRole.ADMIN])`). Those are in the layout, not in `actions.ts`. **VERIFIED.** Whether layout gating covers a direct server-action invocation was not exercised in this recon (would need a running app + session).

### Admin client duplicates

- `src/app/moose-portal/_lib/admin.ts` vs `src/utils/supabase/admin.ts`: **NOT byte-identical** (`cmp`: differ at byte 35, line 2). The only difference is one extra comment line in `src/utils/supabase/admin.ts:2` ("BLESSED INFRA — kept unconsumed by ruling 2026-07-14 … reconcile the duplicates at Phase 3"). Executable code is the same in both. **CHANGED** (vs a "byte-identical" claim) / VERIFIED (code-identical).
- Both files carry the header comment `// src/utils/supabase/admin.ts` at line 1 — including the `_lib` copy.
- Consumers: `_lib/admin.ts` → one importer, `src/app/moose-portal/users/actions.ts:4`. `utils/supabase/admin.ts` → **zero importers** in `src/` and `scripts/`. **VERIFIED.**

### `src/app/api/auth/login/route.ts`

- GET handler at `:11-27`, commented "Testing the route" (`:10`), runs `supabase.from("posts").select("*")` at `:13`. **VERIFIED.**
- `grep -rni posts supabase/migrations/` → no hits. No migration creates a `posts` table. **VERIFIED (absent).**

### `src/services/owedbook.ts`

- `getKpis`: `underpaid` computed once at `:72-74`; assigned to `commercial_underpaid` at `:76` and to `owed` at `:81`. Same value. **VERIFIED.**
- `getSummary`: `if (!r.pbm) continue;` at `:113` — null-pbm rows skipped. **VERIFIED.**

### Accessibility claims

| Component | Finding | Label |
| --- | --- | --- |
| `src/components/common/DataTable.tsx` sortable `<th>` (`:70-86`) | Has `onClick` (`:72`) and `aria-sort` (`:76`). **No `tabIndex`, no `role`, no `onKeyDown`**; no `<button>` inside the header. | VERIFIED |
| `src/components/layout/AuthedShell.tsx` drawer (`:92-…`) | `role="dialog" aria-modal="true"` (`:93`); Escape-to-close + body scroll lock (`:46-57`). The string "focus" does not appear in the file; no `useRef` imported (`:3`). **No focus move-in, no trap, no restore on close.** | VERIFIED |
| `src/components/common/MultiSelect.tsx` | Click-outside + Escape close (`:27-46`); `aria-haspopup`/`aria-expanded` (`:70-71`); `role="listbox"` (`:85`). One ref, `containerRef` (`:25`), used for click-outside only. **No focus trap, no focus return.** | VERIFIED |

### `src/components/auth/RegisterForm.tsx`

- `handleSubmit` `:67-92`: `await fetch("/api/auth/signup", …)` at `:70` with **no try/catch/finally**. `setIsLoading(false)` exists only on the non-OK branch (`:90`). **VERIFIED.**
- OK path (`:84-86`): `router.refresh()` then `router.push("/owedbook")`. The response body is not read and no session is inspected before the push. **VERIFIED.**
- `console.log("Signup Response: ", response)` at `:82`. **VERIFIED.**

### `next.config.js`

- `headers()` `:13-34`: `source: "/(.*)"` (`:16`); `Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0` (`:19-21`); plus `Pragma: no-cache`, `Expires: 0`. **VERIFIED.**
- `images.remotePatterns` `:6-11`: one entry, `https` / `res.cloudinary.com`. **VERIFIED.**
- `images.unoptimized`: key absent. **NOT FOUND.**
- Also present: `output: "standalone"` (`:3`).

### Debug strings

- `src/app/not-found.tsx:14` → `<p>This is coming from /app</p>`. **VERIFIED.**
- `src/app/(admin)/not-found.tsx:14` → `<p>This is coming from /main</p>`. **VERIFIED.** (Its line-1 comment reads `// app/not-found.tsx`.)

### `MobileNav.tsx` / `UserMenu.tsx`

- `UserMenu.tsx`: no `useAuthStore` import; `role` arrives as a prop (`:21`, `:24`). No `createClient()` / `supabase.auth.getUser()`. **VERIFIED — no store role read.**
- `MobileNav.tsx`: imports `useAuthStore` (`:8`); sole use is the logout action `useAuthStore.getState().logout()` (`:33`). `role` is a prop (`:22`, `:25`). No `createClient()` / `getUser()` in the body. **VERIFIED — no store role read remains.** Any review claim of a persisted-store role read in these two files is **CHANGED** (closed by FIX-001, `KIP_REGISTRY.md:51-59`).

---

## R2-ADDENDUM — CLAIMS IN THE REVIEWS THAT THE MISSION DID NOT LIST

| Claim | Current disk | Label |
| --- | --- | --- |
| **F4** page-local sort | `OwedBookScreen.tsx:127-137` sorts `rows` (the current page) client-side; comment `:124-125` "Client-side sort of the current page (server-side sort is a Phase-3 concern — the service contract takes no sort param yet)". `getRows` paginates with no sort param (`services/owedbook.ts:85-107`). | VERIFIED |
| **F8** federal sign / Summary federal ≈ 0 | Fixtures (`src/mocks/owedbook.ts`, 150 rows): `federal_diff` negative on **132**, positive on **7**, null on **11**. Fable wrote "~143/150 negative" — 143 = non-positive (132 + 11 null). `getSummary` counts only `federal_diff > 0` (`services/owedbook.ts:117`). Certified wrapper returns hard `0` (`0046:62`). | VERIFIED (count wording CHANGED: 132 negative + 11 null) |
| **F10** inert "Report" button | `src/components/owedbook/columns.tsx:17-19` — `<button type="button" …>Report</button>` with no `onClick`; comment `:11` "real view/download is Phase 5/6". | VERIFIED |
| **A-006** upload/refresh do not invalidate; no catch | `FilterRail.tsx:59-66` (`handleFile`) and `:68-72` (`handleRefresh`): bare `await`, no try/catch/finally, only rail-local status set. `OwedBookScreen.tsx:80-82`: KPI rejection → `setKpis(ZERO_KPIS)`, no error state. `reloadKey` is local to the Retry control (`:67,114,227`). `OwedBookContext.tsx:48-57`: PBM options load once (`[]` deps), `.catch(() => {})`. | VERIFIED |
| **A-004** dollar figures ($4,088.54 vs $4,068.27; $20.27 gap) | Mechanism VERIFIED (R2/R5). Four null-pbm fixture rows confirmed (`grep -c 'pbm: null'` = 4). The dollar totals were **not re-computed** in this recon. | VERIFIED (mechanism) / figures not re-run |
| **A-003** lockfile line refs + Dockerfile | Versions match (R6). Line numbers differ here: next `package-lock.json:10183` (Astra `:10052`), sharp `:11644` (`:11389`), libvips-musl `:1356` (`:1362`) — a different lockfile state from Astra's export. Astra cites a "Dockerfile's Node Alpine runtime": **no Dockerfile exists in this repo.** | CHANGED (line refs) / NOT FOUND (Dockerfile) |

---

## R3 CONTROLLED BUILD

- `node_modules/` present. **VERIFIED.** Next.js 16.2.12 (Turbopack).
- **Deviation from the literal instruction, stated plainly:** `.env.local` exists and Next loads it automatically (build log: `- Environments: .env.local`). Merely *unsetting* the two keys in the shell would therefore have been a normal build with the real values — uninformative, and a possible route to a live Supabase contact. No Supabase keys are set in the shell. I instead set both keys to the **empty string for that command only**; Next's env loader does not override a key already defined in `process.env`, so the build genuinely saw blank values. `.env.local` was not touched.

| Run | Env for the two keys | Exit | Result | Label |
| --- | --- | --- | --- | --- |
| Build A | both blank | **0** | No error line. Compiled 7.0s, TypeScript 3.0s, 16/16 static pages. 22 routes. | VERIFIED |
| Build B | URL `https://placeholder.supabase.co`, key `placeholder` | **0** | **22 routes**, route table identical to Build A. | VERIFIED |

Build A exiting 0 means the production build does **not** fail when the two public Supabase keys are blank. (Boot-time validation is separate: `src/instrumentation.ts:11-32` throws at server start if any of four required keys is missing/blank.)

Only non-route output: a Browserslist "caniuse-lite is 13 months old" notice.

**`next start`** on free port 36055, all three Supabase keys overridden with placeholders (no live contact possible). Log: `⚠ "next start" does not work with "output: standalone" configuration` then `✓ Environment validated`, `Ready in 146ms`.

| Request | Status | Cache-Control | Label |
| --- | --- | --- | --- |
| `HEAD /` | 200 | `no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0` (+ `Pragma: no-cache`, `Expires: 0`) | VERIFIED |
| `HEAD /_next/static/chunks/02lo-h1-7oik6.js` | 200, `application/javascript` | **`no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0`** (+ `Pragma: no-cache`, `Expires: 0`) | VERIFIED |

The hashed static chunk is served with the same no-store header as pages. Server stopped; confirmed no `next start`/`next-server` process and nothing listening on 36055. Not tested: the `node .next/standalone/server.js` path.

---

## R4 SQL: CANONICAL VS LEGACY

**`grep -rn handle_new_user supabase/migrations/`** → two hits, both in `0001_baseline_acknowledge.sql:30-31`, and both are an **existence assertion only** (`pg_proc` lookup, raise if missing). **No migration in the chain creates or replaces `handle_new_user`.** The chain never asserts its body. **VERIFIED.**

Every on-disk definition of the function:

| File | `CREATE OR REPLACE` at | Name key read | Role source |
| --- | --- | --- | --- |
| `supabase/setup.sql` | `:87` | `raw_user_meta_data ->> 'name'` (`:103`) | hardcoded `'member'` (`:95-96`) |
| `docs/setup.sql` | `:87` | `'name'` (`:103`) | hardcoded `'member'` — **byte-identical to `supabase/setup.sql`** (`cmp`) |
| `docs/migration_add_profiles.sql` | `:72` | `'full_name'` (`:98`) | `raw_user_meta_data ->> 'role'` if present, else `'member'` (`:82-85`) |
| `scripts/db-bootstrap-baseline.sql` (scratch-only, header `:1-8`) | `:67` | `'full_name'` (`:90`) | metadata role, else `'member'` (`:76-79`) — labelled "smart-trigger version (migration STEP 4, live)" (`:66`) |
| `docs/DATABASE_SETUP.md` (doc code block) | `:105` | `'name'` (`:119`) | hardcoded `'member'` (`:112-113`) |

What the app sends: signup route sets `full_name` (`src/app/api/auth/signup/route.ts:14`); `addMember` sets `full_name` + `role: "member"` (`src/app/moose-portal/users/actions.ts:185-188`).

**Legacy files:** `docs/setup.sql`, `supabase/setup.sql`, `docs/migration_add_profiles.sql` all still exist. **VERIFIED.**

**`docs/DATABASE_SETUP.md` still directs existing installs to the profiles migration. VERIFIED.** Quote, `:22-25`:

> ### Already have `user_roles` with existing data?
> Run **[`docs/migration_add_profiles.sql`](./migration_add_profiles.sql)** instead.
> It only adds the `profiles` table, backfills existing users, and updates the trigger. Safe to run on a live database.

Same doc's own Step-4 block (`:105-124`) shows the *setup.sql* body (`'name'`, hardcoded member) — the doc contains both lineages.

**Plainly, at the 2-table baseline (migrations NOT applied):** `agent_docs/DB_BASELINE.md` does **not** describe the trigger body. Its entire trigger description is `:15`:

> `public.profiles` | Profile fields; created by `handle_new_user()` trigger on auth signup

Its interpretation, `:29-31`:

> The live schema is **`supabase/setup.sql` base + `docs/migration_add_profiles.sql` overlay, applied in sequence**. The migration's profile policies are the live ones…

By that interpretation the overlay's `CREATE OR REPLACE` (`migration_add_profiles.sql:72`) would be the live body — `full_name` key, metadata-role-else-member. That is an **inference from the doc, not catalog evidence**: no `prosrc` capture exists on disk (`BIM-001 X0_EVIDENCE.md:11` lists function *names* only), and the chain asserts existence only. **NEEDS DIRECTOR** — a live `pg_get_functiondef` is the only thing that settles it, and live calls are outside this recon.

---

## R5 CERTIFIED WRAPPER CONTRACTS

Sources: `agent_docs/ACTIONS/BIM-003-CYBER-PHARMA/evidence/WRAPPER_CONTRACT.md`, `supabase/migrations/0044_owedbook_kpis.sql`, `0046_owedbook_summary.sql`, `0047_owedbook_pbm_options.sql`.

**`owedbook_kpis` — SQL expressions (`0044:67-77`):**

| Column | Expression | Line |
| --- | --- | --- |
| `commercial_underpaid` | `round(coalesce(sum(ud.owed) filter (where ud.owed > 0), 0), 2)::numeric` | `:68` |
| `commercial_scripts` | `count(*)::bigint` | `:69` |
| `updated_difference` | `round(coalesce(sum(ud.difference), 0), 2)::numeric` | `:70` |
| `owed` | `round(coalesce(sum(ud.owed) filter (where ud.owed > 0), 0), 2)::numeric` | `:71` |

- **`owed` and `commercial_underpaid` are computed identically** — `:68` and `:71` are the same expression. Contract states it as design: `WRAPPER_CONTRACT.md:42` "`owed` = `commercial_underpaid` (mock :81)"; migration header `0044:8`. **VERIFIED.**
- **Federal/PBM rows excluded? No.** The WHERE (`:73-77`) is tenant fence + the four caller filters only. No federal predicate, no `insurance is not null`. `count(*)` counts every filtered row, including NULL-insurance rows. **VERIFIED.**

**`owedbook_summary` — NULL pbm (`0046:58-71`):** **dropped.** `and ud.insurance is not null` at `:65`, then `group by ud.insurance` (`:70`). Not bucketed, not labelled. Header `0046:5` "Rows with no PBM are skipped (mock :113)"; `WRAPPER_CONTRACT.md:63`. `federal_dollars` is a hard `0::numeric` (`:62`). Order `order by 2 desc, 1` (`:71`). **VERIFIED.**

**Mock ↔ wrapper divergences:**

| # | Mock (`src/services/owedbook.ts`) | Wrapper | Documented? |
| --- | --- | --- | --- |
| D1 | `federal_dollars` = sum of positive `federal_diff` (`:117`) | hard `0` (`0046:62`) | Yes — A-3, `WRAPPER_CONTRACT.md:63` |
| D2 | `updated_difference` sums field `updated_difference ?? 0` (`:78-80`) | sums stored column `difference` (`0044:70`) | Yes — column mapping, `WRAPPER_CONTRACT.md:24,41`. Mapping, not a formula change. |
| D3 | Summary sort: `commercial_dollars` desc, no tiebreak (`:126`) | `order by 2 desc, 1` — adds PBM-name tiebreak (`0046:71`) | Not called out in the contract |
| D4 | Skips pbm by **truthiness** — `!r.pbm` (`:113`), `Boolean(p)` (`:131`): empty string is skipped | `is not null` only (`0046:65`, `0047:50`): an empty-string `insurance` is kept | Not called out in the contract |
| D5 | Rounding via JS float `toFixed(2)` (`:56`) | SQL `round(numeric, 2)` | Not called out |

KPI formulas themselves (`commercial_underpaid`, `commercial_scripts`, `owed`): **no divergence.** PBM-filter null handling matches (mock `:64` excludes null pbm when a PBM filter is active; SQL `= any` excludes NULL likewise).

---

## R6 DEPENDENCIES

`npm ls … --depth=0` prints only `next@16.2.12` — `sharp` is not a direct dependency. At full depth:

| Package | Version | Note |
| --- | --- | --- |
| `next` | **16.2.12** | `package.json:41` `^16.2.1` |
| `sharp` | **0.35.3** "overridden" | transitive via `next`; pinned by `overrides` `package.json:75` `^0.35.3` |
| `@img/sharp-libvips-linuxmusl-x64` | **1.3.2** | |
| `@img/sharp-libvips-linux-x64` | 1.3.2 | |

**`npm audit --json`** (registry data as returned today; exit 1): **6 total — critical 1 · high 3 · moderate 1 · low 1 · info 0.**

Advisories touching next / sharp / libvips only:

| Package | Advisory | Title (as returned) | Severity | Affected range |
| --- | --- | --- | --- | --- |
| `next` | GHSA-p293-qw3h-jr36 (1193676) | Unauthenticated RCE on windows-hosted servers | critical | `>=16.0.0 <16.3.3` |
| `next` | GHSA-2xp9-vwfh-vxw4 (1193732) | Unauthenticated RCE in Image Optimization API when AVIF files are used | critical | `>=16.0.0 <16.3.3` |
| `sharp` | GHSA-rgj7-g3m4-5g8c (1193725) | Vulnerabilities in libheif (GHSA-g89c-p67h-r497, GHSA-2jg2-4ch7-h545) | high | `<0.35.4` |

No advisory is filed directly against an `@img/sharp-libvips-*` package. `fixAvailable: true` for both `next` and `sharp`. Other flagged packages (not detailed per mission): `baseline-browser-mapping`, `browserslist`, `js-yaml`, `postcss-selector-parser`. Installed `next@16.2.12` and `sharp@0.35.3` both fall inside the affected ranges. **VERIFIED** (as of this audit run).

**`next/image` users:** `src/app/(public)/HomePageContent.tsx:1` · `src/components/global/NavbarLoginReg.tsx:1` · `src/components/global/NavbarHome.tsx:1` · `src/components/global/Navbar.tsx:4`. (`src/proxy.ts:10` and `src/__tests__/proxy.test.ts:28` mention `_next/image` in the matcher only.)

---

## R7 TESTS

- **26 → 28 suites:** `git diff --name-status 6f6e63d..HEAD` → exactly two, both Added: `src/__tests__/global/MobileNav.test.tsx`, `src/__tests__/global/UserMenu.test.tsx`. **VERIFIED.** (jest not executed — not in the mission.)
- **`src/__tests__/services/owedbook.test.ts`:**
  - `:23` → `expect(kpis.owed).toBe(kpis.commercial_underpaid);` — requires equality. **VERIFIED.**
  - `:10-12` → `it("lists all 7 PBMs, sorted, no nulls", …)` / `expect(pbms).toHaveLength(7);` — assumes exactly seven. **VERIFIED.**
  - `:56` → `expect(summary.length).toBeLessThanOrEqual(7);`
- **Playwright:** no `playwright.config.*`; no `*.spec.ts`, no `e2e/` dir. But `@playwright/test ^1.59.1` is a devDependency (`package.json:52`) with scripts `test:e2e` / `test:e2e:ui` (`:11-12`). Config **NOT FOUND**; specs **NOT FOUND**.

---

## R8 CONTRACTS ON DISK (re-answered in Revision 2)

Documents now on disk: `agent_docs/PHASE_2.1/UI_SPEC.md` (v1.0, 153 lines), `PHASE_2.1/DATA_CONTRACT.md` (v1.0, 154 lines), `PHASE_2.2/UI_SPEC.md` (115 lines), `PHASE_2.2/DATA_CONTRACT.md` (255 lines; admin-portal view-models — no OwedBook/pbm content).

- **Keyboard-operable sortable headers + focus trapping: VERIFIED** — `agent_docs/PHASE_2.1/UI_SPEC.md:132-134` (§8 Accessibility):

  > - DataTable: proper `<table>` semantics (or ARIA grid), sortable headers keyboard-operable, sort state announced.
  > - MultiSelect: keyboard nav, checkboxes labeled, panel focus-trapped.

  and `:134` "Filter drawer: focus-trap + Escape to close." (Astra cites `UI_SPEC.md:237–242` — a longer, later version than the file on disk.)

- **"pbm may be null / renders as em dash": NOT FOUND in the on-disk contract.** v1.0 declares it non-nullable — `agent_docs/PHASE_2.1/DATA_CONTRACT.md:43-44`:

  > `status: OwedStatus | null; // null shows as "—"`
  > `pbm: string;               // PBM name, for filtering`

  Only `status` carries the em-dash rule in v1.0. The nullable-pbm statement exists on disk only in code, `src/types/OwedBook.ts:3-4,10`, which cites a later "DATA_CONTRACT §4". **NEEDS DIRECTOR** — supply the later contract version (the one matching UI_SPEC v1.3/v1.4) or rule which governs.

- **Summary aggregate definition: VERIFIED** — `agent_docs/PHASE_2.1/DATA_CONTRACT.md:86-91` (`OwedBookSummaryRow { pbm: string; commercial_dollars; federal_dollars }`) and §5 `:117,120`:

  > `* Summary-tab aggregate (PBM → commercial/federal dollars).`
  > `getSummary(filters: OwedBookFilters): Promise<OwedBookSummaryRow[]>;`

  Also `:144` "Whether Summary aggregates server-side or client-side at real scale → Phase 3." The contract defines shape and grouping only; it says nothing about rows with no PBM.

---

## R9 DEPLOY FACTS (names only)

> **Revision 2:** the Director has waived deployment facts. Findings below stand as recorded; no follow-up owed.

- `cloudbuild.yaml`: **NOT FOUND.** `deploy.sh`: **NOT FOUND.** Also absent: `Dockerfile`, any `*.yml`/`*.yaml`, `vercel.json`, `.github/`. The only deploy-adjacent file is `.dockerignore`. No deploy-time value for `NEXT_PUBLIC_ENABLE_MOOSE_PORTAL` exists on disk. **NEEDS DIRECTOR** (where is it set?).
- `.env.example` keys: `NEXT_PUBLIC_ENABLE_MOOSE_PORTAL` · `NEXT_PUBLIC_SITE_URL` · `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` · `NEXT_PUBLIC_SUPABASE_URL` · `SUPABASE_SECRET_KEY`. **Moose flag key present** at `.env.example:7`, example literal `false` (non-secret). **VERIFIED.**
- For reference, `.env.local` key names: the five above plus `NEXT_PUBLIC_API_BASE_URL`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `PROTO06_DB_URL`, `RLS_REPLICA_DB_URL`, `RLS_REPLICA_PUBLISHABLE_KEY`, `RLS_REPLICA_SECRET_KEY`, `RLS_REPLICA_SUPABASE_URL`.
- Flag consumers: `src/app/moose-portal/layout.tsx:15`, `src/components/global/Navbar.tsx:49`.

---

## CLOSING TABLE (Revision 2 — Finding IDs mapped from the two reviews)

Sources: `agent_docs/FABLE_CODE_REVIEW.md` §7 (`:98-110`), `agent_docs/ASTRA_CODE_REVIEW.md` §7 (`:80-87`). Status is the state of the claim **on current disk**.

| Finding | Claim (as the review states it) | Status | Evidence |
| --- | --- | --- | --- |
| **F1** | Moose privileged Server Actions have no in-action authorization | VERIFIED | `moose-portal/users/actions.ts:27,92,126,158,178`; gates only in `layout.tsx:15,17` |
| **F2** | Login route GET queries non-existent `posts` | VERIFIED | `api/auth/login/route.ts:13`; no `posts` in `supabase/migrations/` |
| **F3** | KPI "Owed" always equals "Commercial Underpaid" | VERIFIED — and the certified wrapper encodes the same equality | `services/owedbook.ts:76,81`; `0044:68,71`; `WRAPPER_CONTRACT.md:42`; test pin `owedbook.test.ts:23` |
| **F4** | Column sort reorders only the current page | VERIFIED | `OwedBookScreen.tsx:124-137` |
| **F5** | Persisted `useAuthStore.role` read in MobileNav/UserMenu can be stale | CHANGED — no longer present (FIX-001) | `MobileNav.tsx:22,33`; `UserMenu.tsx:21`; `KIP_REGISTRY.md:51-59` |
| **F6** | Global `no-store` header covers static assets | VERIFIED — now measured at runtime (Fable could not) | `next.config.js:16-21`; R3 `HEAD` on `/_next/static/chunks/*.js` |
| **F7** | No-env build fails (`/access-denied` prerender builds a Supabase client) | CHANGED — blank-env build exits 0; `/access-denied` is now dynamic (ƒ); the client `createClient()` calls are gone | R3 Build A; route table |
| **F8** | Federal sign convention diverges; Summary federal ≈ 0 | VERIFIED (132 negative / 7 positive / 11 null; Fable's "~143" = non-positive) | `src/mocks/owedbook.ts`; `services/owedbook.ts:117`; `0046:62` |
| **F9** | Dependency advisories incl. Next.js critical | VERIFIED for `next`/`sharp`; total CHANGED 8 → 6 (1 critical · 3 high · 1 moderate · 1 low) | R6 |
| **F10** | Sortable headers mouse-only; inert Report button | VERIFIED (both halves) | `DataTable.tsx:70-86`; `columns.tsx:17-19` |
| **F11** | Signup pushes to `/owedbook` without checking session | VERIFIED (code path); confirmation setting NEEDS DIRECTOR | `RegisterForm.tsx:84-86` |
| **F12** | Duplicate service-role admin client | VERIFIED as duplicate; "identical" CHANGED — one comment line differs; `utils` copy has zero importers | `utils/supabase/admin.ts:2`; `actions.ts:4` |
| **F13** | Debug strings in not-found pages; `console.log` in RegisterForm | VERIFIED | `app/not-found.tsx:14`; `app/(admin)/not-found.tsx:14`; `RegisterForm.tsx:82` |
| **A-001** | Moose actions omit operation-level authorization (incl. no flag check, no target-role limit on delete) | VERIFIED | as F1; `actions.ts:155,160` |
| **A-002** | Upgrade trigger grants role from user-controlled signup metadata | VERIFIED in SQL on disk; **installed body NEEDS DIRECTOR** | `docs/migration_add_profiles.sql:82-85`; `scripts/db-bootstrap-baseline.sql:76-79`; `DB_BASELINE.md:29-31` (inference only) |
| **A-003** | Locked AVIF stack (next 16.2.12 / sharp 0.35.3 / libvips-musl 1.3.2) matches RCE advisory; broad `res.cloudinary.com` pattern; `_next/image` outside proxy matcher | VERIFIED (versions, config, matcher); lock line refs CHANGED; Dockerfile NOT FOUND | R6; `next.config.js:6-11`; `src/proxy.ts:10`; R2-ADDENDUM |
| **A-004** | Missing-PBM claims disappear from Summary dollars | VERIFIED (mechanism, mock and certified wrapper both drop them); dollar figures not re-run | `services/owedbook.ts:113`; `0046:65`; 4 null-pbm fixture rows |
| **A-005** | Ledger controls omit required keyboard/focus behaviour | VERIFIED — requirement located on disk | `DataTable.tsx:70-86`; `AuthedShell.tsx:46-57,93`; `MultiSelect.tsx:25-46`; `PHASE_2.1/UI_SPEC.md:132-134` |
| **A-006** | Ledger components cannot support a service-only swap (no invalidation, no catch, KPI error → zeros) | VERIFIED | `FilterRail.tsx:59-72`; `OwedBookScreen.tsx:80-82,67,227`; `OwedBookContext.tsx:48-57` |
| **A-007** | Signup: unguarded fetch, loading never released on reject, OK path ignores session | VERIFIED | `RegisterForm.tsx:70,84-86,90` |
| **A-008** | Fresh-setup trigger reads `name` while the app sends `full_name` | VERIFIED on disk; installed body NEEDS DIRECTOR | `supabase/setup.sql:103`; `api/auth/signup/route.ts:14`; `actions.ts:185-188` |

**Recon-only observations (no review ID):** `f1113177` absent from this repo (R1) · mock↔wrapper divergences D3–D5 not called out in the contract (R5) · Playwright installed with no config/specs (R7) · 7-PBM test pin `owedbook.test.ts:12` (R7) · `images.unoptimized` absent (R2) · dropped Phase-2.1 specs are v1.0 (R8).

## OPEN ITEMS FOR THE DIRECTOR

1. **Live `handle_new_user` body** — settles A-002 and A-008. Director runs, read-only, in the Supabase SQL editor on the dev backend: `select pg_get_functiondef('public.handle_new_user()'::regprocedure);`
2. **Later spec versions** — the on-disk Phase-2.1 UI_SPEC / DATA_CONTRACT are v1.0; code and Astra cite v1.3/v1.4 and a nullable `pbm`. Which version governs, and can it be placed on disk?
3. **Email-confirmation setting** on the Supabase project — decides whether the second half of F11 / A-007 is live.
4. ~~Finding-ID mapping~~ — **closed** in Revision 2. ~~Commit `f1113177`~~ — **explained** (different repo). ~~Deploy value of the moose flag~~ — **waived** by the Director.
