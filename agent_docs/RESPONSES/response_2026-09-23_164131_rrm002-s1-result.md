# RRM-002-CYBER-PHARMA — Stage S1 complete: Summary-tab disclosure (R-015)

**Engineer** · 2026-09-23 · branch `phase-3-rrm002` · input SHA `6f543a8` · baseline `1cd6e465ebbfeb0842738fcbab1ffbe65e2dbe6b` · no stop condition hit: no formula beyond `round2(K − S)`, no service change, no fixture edit.

## Changes made

| File | Change |
|---|---|
| `src/components/owedbook/format.ts` | +`export const round2 = (n: number): number => +n.toFixed(2);` — same expression as the frozen service `:56` (A-01) |
| `src/components/owedbook/SummaryUnattributedNote.tsx` | NEW. Props `underpaid` (K) + `summary` rows. `S = Σ commercial_dollars`, `gap = round2(underpaid − S)`; returns null when `gap < 0.01`; else one `<p data-testid="summary-unattributed-note">` with `usd(gap)` + the D3 copy, held in one constant |
| `src/components/owedbook/OwedBookScreen.tsx` | +24/−2: `OwedBookFilters` type import · note import · two tags `kpiFilters`/`summaryFilters` · KPI effect `.then/.catch` now sets the tag beside the value (catch keeps `ZERO_KPIS`, tag → `null`) · summary branch sets `summaryFilters` · one render block after the table gated on `isSummary && !loading && !error && kpiFilters === filters && summaryFilters === filters` |
| `src/__tests__/owedbook/SummaryUnattributedNote.test.tsx` | NEW, 6 tests: text + single testid; 0.01 boundary; gap 0; S > K by 0.004; K − S = 0.004; gap −5.00 (no `$0.00`) |
| `src/__tests__/owedbook/OwedBookScreen.disclosure.test.tsx` | NEW, 8 tests. Real service: AC-101 unfiltered and `2026-06-01` date range, expected **derived** from `owedBookFixtures` (`usd(round2(Σ owed where pbm === null && owed > 0 [&& in range]))`, no literal); AC-102 named-PBM-only (all seven from `getPbmOptions()`); AC-104 absent on the three other tabs, gone on switch away, back after the skeleton with a new request (A-03). Spied service: rejected `getKpis` → no note, tiles `$0.00`; pending summary → skeleton, no note; summary deferred across a filters change then resolved → no note from the stale pair; K for A with S for B → no note until K for B resolves |
| `agent_docs/ACTIONS/RRM-002-CYBER-PHARMA/evidence/S1_diffs.txt` | NEW (temp-then-move): AC-301, AC-106, AC-302, AC-303 transcripts + full `OwedBookScreen.tsx` and `format.ts` diffs |
| `EXECUTION_LOG.md` S1 · `CHANGELOG.md` · session log | bookkeeping |

## Board

| Check | Result |
|---|---|
| `rm -rf .next && next build`, blank Supabase env | exit 0 · 17 routes |
| same, placeholder env (`https://placeholder.invalid`, placeholder keys) | exit 0 · 17 routes — `/`, `/_not-found`, `/access-denied`, `/admin-portal`, `/admin-portal/audit`, `/admin-portal/billing`, `/admin-portal/settings`, `/admin-portal/stores/[id]`, `/admin-portal/stores/[id]/invite`, `/api/auth/confirm`, `/api/auth/login`, `/api/auth/logout`, `/auth`, `/error`, `/icon.png`, `/owedbook`, `/profile` |
| `npx tsc --noEmit` (after the fresh build) | 0 errors |
| `npx eslint .` | 0 errors / 35 warnings — same count as RRM-001's board. The two in `OwedBookScreen.tsx` (:79, :105, `set-state-in-effect`) exist at baseline (:73, :91); lines shifted by the additions, none new |
| `npx jest --ci` | 31 suites / 144 tests / 0 skipped (baseline 29/130; +2 suites, +14 tests, all new) |
| AC-301 `git diff --stat <baseline> -- <preserved list>` | **empty** |
| AC-106 `git diff <baseline> -- src/services/owedbook.ts src/types/OwedBook.ts` | **empty** |
| AC-302 `owedbook.test.ts` diff · tracked `__tests__` diff | **empty** · **empty**; only two untracked additions |
| AC-303 `cancelled` grep · sort-line diff grep | both read effects guarded · no `sort` line in the diff |
| `git diff --stat <baseline> -- src/` | `OwedBookScreen.tsx` +24/−2 · `format.ts` +4 · 3 new files |

Deviation, test-only: two `findByText` calls became `findAllByText` after the first run — the DataTable paints a table and a card layout, and the two KPI tiles share the R-003 value. Env: placeholder/blank values scoped per command, nothing exported, `.env.local` not read for values and not edited, no live Supabase call; `.next/` (gitignored) left as the placeholder-env build.

## OwedBookScreen.tsx diff vs baseline (for the QA Lead)

```diff
@@ -7,6 +7,7 @@ import DataTable from "@/components/common/DataTable";
 import EmptyState from "@/components/common/EmptyState";
 import { owedBookService } from "@/services/owedbook";
 import type {
+  OwedBookFilters,
   OwedBookKpis,
   OwedBookPage,
   OwedBookRow,
@@ -14,6 +15,7 @@ import type {
   OwedTab,
 } from "@/types/OwedBook";
 import KpiTiles from "./KpiTiles";
+import SummaryUnattributedNote from "./SummaryUnattributedNote";
 import { useOwedBook, countActiveFilters } from "./OwedBookContext";
 import {
   COMMERCIAL_COLUMNS,
@@ -62,6 +64,10 @@ const OwedBookScreen = () => {
   const [kpis, setKpis] = useState<OwedBookKpis>(ZERO_KPIS);
   const [pageData, setPageData] = useState<OwedBookPage | null>(null);
   const [summaryRows, setSummaryRows] = useState<OwedBookSummaryRow[]>([]);
+  // RRM-002 AC-103: which `filters` reference each aggregate was resolved for.
+  // The disclosure renders only when both tags === the current `filters`.
+  const [kpiFilters, setKpiFilters] = useState<OwedBookFilters | null>(null);
+  const [summaryFilters, setSummaryFilters] = useState<OwedBookFilters | null>(null);
   const [loading, setLoading] = useState(true);
   const [error, setError] = useState<string | null>(null);
   const [reloadKey, setReloadKey] = useState(0);
@@ -78,8 +84,16 @@ const OwedBookScreen = () => {
     let cancelled = false;
     owedBookService
       .getKpis(filters)
-      .then((k) => !cancelled && setKpis(k))
-      .catch(() => !cancelled && setKpis(ZERO_KPIS));
+      .then((k) => {
+        if (cancelled) return;
+        setKpis(k);
+        setKpiFilters(filters);
+      })
+      .catch(() => {
+        if (cancelled) return;
+        setKpis(ZERO_KPIS);
+        setKpiFilters(null);
+      });
     return () => {
       cancelled = true;
     };
@@ -97,6 +111,7 @@ const OwedBookScreen = () => {
           if (!cancelled) {
             setSummaryRows(s);
             setPageData(null);
+            setSummaryFilters(filters);
           }
         } else {
           const p = await owedBookService.getRows(activeTab, filters, page);
@@ -250,6 +265,11 @@ const OwedBookScreen = () => {
           />
         )}
       </div>
+
+      {/* RRM-002 D3 disclosure — Summary tab only, same-filters pair only (AC-101–104). */}
+      {isSummary && !loading && !error && kpiFilters === filters && summaryFilters === filters && (
+        <SummaryUnattributedNote underpaid={kpis.commercial_underpaid} summary={summaryRows} />
+      )}
     </div>
   );
 };
```

## Things I did not touch

`src/services/owedbook.ts` · `src/types/OwedBook.ts` · `columns.tsx` · `KpiTiles.tsx` · `FilterRail.tsx` · `OwedBookContext.tsx` · `src/components/common/**` · `src/app/**` · `src/mocks/owedbook.ts` (E-12 is S2) · `owedbook.test.ts` and every existing suite · `supabase/**` · `scripts/**` · `package.json`/lockfile · `next.config.js` · `RECOVERY.md` · `.env.local`.

## Potential concerns

- The 35-warning count is unchanged, but the rule name reads `Error: Calling setState synchronously within an effect`; that is the plugin's message text at warning severity, present at baseline. Not introduced here.
- AC-304 (authenticated browser check, desktop/375px, light/dark) is QA's; not run here.

## Tests to run

`npx jest --ci src/__tests__/owedbook` for the 14 new tests in isolation; `evidence/S1_diffs.txt` for every diff claim.
