No server was started, so there is nothing for Cody to shut down.

Standing Director instruction: **Tony alone starts and stops application servers, just as Tony alone controls Git.** Cody must not start, restart, stop, replace, or reconfigure an application server. After Tony identifies the running local server and port, Cody prepares/uses the browser and gives Tony one credential action at a time, stating the required existing account role and login URL. Credentials stay in the browser.

For the pending AC-304 walk, once Tony starts the normal-local-config server, he should tell Cody the port. Cody will first open `/auth` and request the existing ADMIN account.
