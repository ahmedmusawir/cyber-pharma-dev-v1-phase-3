# Session Log: 2026-09-01

## Project Context

- **Project:** CYBER_PHARMA — cyber-pharma-dev-v1-phase-3
- **Tool:** Claude Code
- **Goal:** BIM-002-CYBER-PHARMA (RLS policies) — module staged; awaiting the Operator launch line. No task assigned yet.

## Starting State

- **Branch:** `phase-3-bim002` @ `aca3d05` ("1sep2026 - bim002 has begun") — working tree CLEAN.
- **Last Working Feature:** BIM-001 CLOSED (Gate Q PASS, certified `9f8c80d`, zero rework) — sixteen-table schema chain, tooling, regenerated types. Board at close: build 22 routes · tsc clean · jest 28/128/0.
- **Campaign board:** BIM-000 ✅ · FIX-001 ✅ · BIM-001 ✅ · PROTO 06 rig CLOSED (transfers consumed) → **BIM-002 next**.

### New since last session (Director/Architect staging, all committed)

- `agent_docs/ACTIONS/BIM-002-CYBER-PHARMA/` — manager, ACCEPTANCE_SPEC, own AUTHORITY/, evidence/, QA/ (unread — Plan Mode reads them at the launch line).
- **`agent_docs/AUTHORITY/` promoted to campaign level** (doctrine sync `3eba19f`: BIM_PLAYBOOK + CAMPAIGN_MAP v1.1 + README).
- **PROTO 06 artifacts copied forward** to `agent_docs/ACTIONS/PROTO06/`: `TRANSFERS.md` + `FINDINGS.md` alongside the brief and plan. The rig's policies/harness are on main; the `phase-3-proto-6` branch is slated for deletion after BIM-002's harness re-point.

### Known issues / carries into this module

- **KIP-1 — still OPEN/parked** (`server.ts` deprecated cookie adapter + `as any`). FORCED-ENTRY trigger is "touching `src/utils/supabase/server.ts`" — BIM-002 is policy work, so the trigger is **not expected to fire**; will surface immediately if scope drifts into that file. KIP-2 CLOSED 2026-08-27.
- **From PROTO 06 (TRANSFERS §5), owed to BIM-002:** live-session junction revocation (N-3) · volume beyond 6k rows (N-4) · multi-policy fan-out across sixteen tables (N-5) · **`accounts`-level access — no policy pattern exists yet (N-6), the likeliest place BIM-002 needs new design**. Storage leg (N-1) and browser-client leg (N-2) are owed elsewhere, not here.
- **From PROTO 06 findings, binding on this module:** F-1 land SELECT before any write policy on every tenant table · F-8 policy order helpers → SELECTs → writes · F-5 SECURITY DEFINER helper verbatim · F-2 PostgREST 1,000-row cap in any verification · one permissive policy per operation per table.
- Older carries: numbered-color predicate rebuild · QA-FINDING-001 (dark-mode login contrast) · `report_files` fidelity flag · ERRATUM E-2 (DATA_CONTRACT §3 amendment, Director doc-repo staging).
- **Director post-BIM-001 duties status unknown to me:** LIVE apply of the migration chain · E-2 staging · throwaway-credential rotation. Worth confirming before BIM-002 assumes live schema state.

## Session Progress

### [morning] — SESSION OPENED

**Task:** Step-0 protocol — RECOVERY.md read, branch/commits verified (clean at `aca3d05`), KIP registry surfaced (KIP-1 parked, trigger not met), PROTO 06 transfer obligations carried forward, session file created.
**Status:** Ready. Awaiting the BIM-002 launch line → Plan Mode.

### [10:22] — PENDING_APPROVAL — BIM-002 launch plan

**Task:** BIM-002-CYBER-PHARMA (RLS policy campaign). Launch line received; authority read in precedence order; Amendment A-1 honored (PROTO06_DB_URL for X0–X5, RLS_HARNESS_* rename at the port, .env.local never edited).
**X0 EXECUTED AND GREEN** (`evidence/X0_catalog_2026-09-01.log`): scratch wiped in F-6 order → chain 0001–0015 applied → **16 tables, all rowsecurity=true, exactly 3 policies (baseline only), 0 on the fourteen new**; `ensure_rls` present; `storage.objects` policies = 0 (AC11 before-state banked); pooler `aws-1` confirmed; all tenant-key columns verified; **all T-7 indexes already present — no index migration needed**.
**Plan:** `agent_docs/RESPONSES/response_2026-09-01_102200_bim002-launch-plan.md` — 0016 helpers + 0017–0027 (one file per table, 15 new policies), harness port map to `scripts/rls-harness/`, R-B A/B method with 1-hour box and tie-break rule, X1–X7 sequence, 4 per-concern commit groups.
**FLAGS (9):** ⛔1 `apa_memberships` has NO tenant key (only nullable soft-ref `discount_redeemed_business_id`) → recommend deny-all · ⛔2 manager's "17 policies" doesn't reconcile with its own breakdown → X0-confirmed count is **15** · ⛔3 **`proto-06/scripts/` NOT copied forward** — the 4 copied harness files have broken imports and the seeder/one-per-op-checker/one-command-proof are missing; need the copy (I may not check out that branch) · ⛔4 no CI config exists anywhere → propose `.github/workflows/rls-harness.yml`, confirm GitHub Actions · ⛔5 X6 replica not provisioned (BIM-001's `ihg…` replica is a reuse candidate) · 6 `user_data` DELETE=admin is strikeable, building as ruled · 7 carried non-decisions (businesses INSERT/DELETE, subscriptions Phase 7, report_files fidelity) · 8 `AUTHORITY/README.md` vs `AUTHORITY_README.md` on disk · 9 scratch's `rls_auto_enable`/`update_updated_at` are bootstrap reproductions, not the AC8 target.
**Status:** Awaiting approval. No migration or harness file authored.

### [10:55] — X1 GATE: RED (defect found, remedy proven, ratification requested)

**Plan APPROVED** with all flag rulings (apa deny-all · count=15 · proto-06/scripts copied at `8ee84b7` · **no CI, AC13 STRUCK** · replica at X6 · DELETE stays admin · no no-op migration · A/B tie-break approved · A-2 accepted).
**Built:** `supabase/migrations/0016_rls_helpers.sql` — `is_member_of` + `is_admin_of` verbatim TRANSFERS §1.0, **`is_account_member` new per R-A** (junction-derived, never `owner_user_id`), all SECURITY DEFINER + STABLE + `search_path=''` + fully qualified, with assert-then-create preamble. Applied clean to scratch.
**X1 board:** shape checks ✅ (3/3 secdef, volatile='s', search_path pinned) · authenticated EXECUTE ✅ · functional probe ✅ (all three return false, no membership) · policy count unchanged ✅ · **⛔ `anon` HOLDS EXECUTE on all three — AC8 fails.**
**⛔ E-2 DEFECT (inherited from Proto 06's template, not introduced):** `revoke … from anon` is a **no-op** — Postgres grants EXECUTE to PUBLIC by default and anon inherits it (ACL `{=X/postgres,…}`). The rig never caught it: its matrix asserted table access, where anon was denied anyway, masking the grant. Not a data leak (anon's `auth.uid()` is NULL → helpers return false) but a privilege-surface defect on a SECURITY DEFINER function reading a PHI-adjacent table. **Remedy `revoke … from public` proven inside a rolled-back transaction (anon denied `42501`); NOT applied** — the manager pins the helpers as verbatim, so strengthening needs ratification.
**Created:** `ERRATUM.md` — E-1 count=15 (ruled) · E-2 helper grant (pending) · E-3 AC13 struck (ruled, spec annotated at evidence-fill).
**Evidence:** `evidence/X1_AC8_helper_shape_2026-09-01.log`, `evidence/X1_FINDING_anon_execute_via_public.log`
**Report:** `RESPONSES/response_2026-09-01_105500_bim002-X1-gate-report.md`
**STOPPED at X1** awaiting acknowledgement + E-2 ratification. Onward if ratified: TRANSFERS §1.0 erratum + FINDINGS F-10 (BIM-005 inherits the same template).

### [11:30] — X1 GREEN (amended) + X2 A/B COMPLETE

**E-2 RATIFIED → applied:** 0016 now `revoke … from public` ×3 with divergence comments citing E-2. **X1 re-run FULL: GREEN** — amended AC8 satisfied incl. raw-ACL check (no bare `=X/` entry), anon DENIED `42501` live, authenticated/service_role hold EXECUTE. Evidence: `evidence/X1_AC8_helper_shape_AMENDED_2026-09-01.log`. ERRATUM E-2 → RATIFIED+APPLIED; port source left untouched per ruling.
**X2 A/B (R-B):** 100k `user_data` rows seeded (target met), probe = leftover Proto 06 auth identity, impersonated `authenticated`, 3 runs/cell cold-discarded.
- **⚠️ Run 1 INVALID and retained as evidence:** B returned **0 rows** (`One-Time Filter: false`) — its inline subquery reads `user_businesses`, which had RLS + zero policies, so it was **blinded by the junction's own RLS and silently denied everything**. F-1 family, polarity reversed. A is immune (SECURITY DEFINER = the F-5 shield).
- **Run 2 (junction SELECT policy present = real production state), row-count parity asserted first (66,667/33,333 both):** unqualified **A 1,660.5ms / B 30.9ms → B 53.8×**; qualified **A 562.8ms / B 18.5ms → B 30.3×**; buffers 101,640 vs 1,641. Tie-break (≤10%) does not apply.
- **DECISION: B for tenant SELECT** (`user_data`, `report_files`, `businesses`); **A retained** for `is_admin_of` writes, `is_account_member` (inline form would depend on TWO policies), and INSERT/UPDATE `WITH CHECK`.
- **Mandatory hardening:** ordering law extended (helpers → **junction SELECT** → other SELECTs → writes) → **renumber: 0017 = user_businesses**, 0018 accounts, 0019 businesses, 0020 subscriptions, 0021 user_data, 0022 report_files, 0023–0027 reference · assert-then-create in every B file requires a junction SELECT policy · `policy-check.mjs` gains the matching rule.
- Residue cleaned; scratch verified back to post-X1 state (0 rows, 3 policies, 3 helpers).
**Carried to close:** F-11 (with F-10) for TRANSFERS/FINDINGS. **AC16 + AC17 satisfied.**
**Decision file:** `evidence/X2_AB_DECISION.md` · **Report:** `RESPONSES/response_2026-09-01_113000_bim002-X1-green-X2-report.md`
**STOPPED before X3** — awaiting acknowledgement of the decision + renumbering.

### [12:05] — X2 FINAL: formulation C adopted · E-4 defect found and fixed

**Architect ordered C measured before adopting B.** Renumbering + hardening approved (junction-first is law regardless).
**⛔ E-4 — found while creating the C helper: X1's GREEN was an ARTIFACT.** `pg_default_acl` grants function EXECUTE to `anon` **explicitly** — PUBLIC and anon are two independent channels; E-2 fixed one. X1 passed only because v1's `revoke from anon` persisted through v2's `create or replace` (which preserves ACLs). A fresh apply of 0016 brought `anon=X` back on all three → **X5 would have failed AC8.** Fixed: BOTH revokes on every helper; fresh-create verified clean (`anon=false`, impersonated anon `42501`). Applied not held (ratified AC8 unmeetable otherwise; strictly more restrictive) — **wording ratification requested**. Method lesson **F-12**: assert privileges only after a from-scratch apply.
**Formulation C — hypothesis proven both halves:** unqualified **29.589ms (+0.8% vs B)**, qualified **18.274ms (+0.7%)**, buffers **1,641 identical to B**, plan shape identical (`hashed SubPlan` → `ProjectSet` run ONCE, 2 rows), row parity asserted first. **Shield test decisive:** junction policy dropped → A 66,667 ✓ · **B 0 (blinded)** · **C 66,667 ✓**. C = B's speed + A's shield → **C ADOPTED for tenant SELECT** (within-20% rule satisfied).
**Allocation:** C for tenant SELECT (user_data, report_files, businesses) · A `is_admin_of` for role-gated writes · A `is_account_member` for accounts/subscriptions · A `is_member_of` for INSERT/UPDATE WITH CHECK · **B adopted nowhere** (kept in templates as documented anti-pattern).
**`my_business_ids()` is now the 4th helper in 0016**, all AC8 assertions pass fresh.
**Infra fought back twice** (pooler ECONNRESET mid-cell; orphaned idle-in-transaction backend holding a lock on user_data → found via pg_stat_activity, terminated, resumed). Neither affected results.
**Residue cleared:** scratch verified at policies=3, user_data=0, accounts=0, 4 helpers.
**Carries:** F-10/F-11/F-12 to TRANSFERS+FINDINGS at close · **X4: leftover Proto 06 auth.users must be explicitly deleted or ignored** (auth survives public wipe) · APPLY SESSION: re-verify E-4 premise on dev backend. AC16 ✅ AC17 ✅.
**Report:** `RESPONSES/response_2026-09-01_120500_bim002-X2-final.md` · ERRATUM E-4, E-5 added.
**STOPPED before X3** — awaiting acknowledgement + E-4 wording ratification.

### [13:30] — X3 COMPLETE: 11 policy files landed red→green, 18 policies, laws held throughout

**E-4 RATIFIED**, C adopted, AC8 amended (evidence taken after from-scratch apply), process note logged (hold pinned-text fixes going forward).
**Harness ported** to `scripts/rls-harness/` (R-D): `lib/{env,db,verdict}.mjs` · `expectations.json` · `payloads.mjs` · `seed.mjs` · `harness.mjs` (320 cells, real publishable-key sessions, self-cleaning) · `policy-check.mjs` (L1 one-per-op · L2 SELECT-before-write incl. file order · L3 junction-first · L4 Gap-6) · `x3-red-green.mjs` (ritual driver).
**Baseline re-proven:** 320 cells GREEN with only the 3 baseline policies granting anything.
**11/11 steps landed red→green**, driver aborts if a step is green before its migration (a policy that never went red proves nothing). Laws checked after EVERY landing. Ledger: `evidence/X3_red_green_ledger.json`.
**Final inventory: 18 policies = 15 new + 3 baseline** — exactly E-1's ruled count. `apa_memberships`/`pending_registrations`/`audit_logs` zero (deny-all by design); `storage.objects` zero (R-E).
**Row scoping exact** (`evidence/X3_row_scoping_check.log`): ownerA 400 (A1+A2) · staffA 200 (A1) · ownerB 200 (B1) · multiStore 400 (A1+B1, both accounts) · anon 0 — measured after TWO consecutive matrix runs = idempotence proof.
**Two harness defects found and fixed (no policy defects):**
- **H-1 not idempotent** — probes with INSERT-allowed/DELETE-denied left rows behind (426 vs 400) and a permitted UPDATE permanently renamed `Store A1`. Fixed: harness tracks its own inserts, service-role-cleans what its DELETE probe couldn't, restores mutated seeded rows (`payloads.restore`).
- **H-2 `accounts.owner_user_id` has NO ON DELETE CASCADE** — `deleteUser` fails for any account owner, so the seed's auth purge died half-way and orphaned identities' sign-in failed **silently** → queries ran as anon. Fixed: public data deleted BEFORE auth purge (order is load-bearing, commented).
**Carried flags added:** account-owner deletion is blocked in production by that FK (decide deliberately at BIM-004/Phase 4) · 4th instrument defect this campaign (my ad-hoc scoping script ignored sign-in errors; harness.mjs fails closed correctly).
**Report:** `RESPONSES/response_2026-09-01_133000_bim002-X3-complete.md`
**STOPPED before X4.**

### [14:15] — X4 COMPLETE: 320 cells + 28 attacks + R-C proven · ZERO ground-truth mismatches

**Rulings applied:** H-1/H-2 accepted as harness-internal · **CF-1** recorded (accounts.owner_user_id no ON DELETE → routed BIM-004/Phase 4) · instrument defect #4 logged, fail-closed-on-auth law restated · **row-scoping promoted to permanent named assertion set** (`scripts/rls-harness/scoping.mjs`).
**Built:** `scoping.mjs` (declared expectations, fails closed on sign-in AND asserts session.user.id matches intended identity) · `attacks.mjs` (28 cases, every mutation ground-truthed via service role) · `revocation.mjs` (R-C).
**Results:** matrix **320/320 GREEN** · scoping **EXACT** (400/200/200/400/0) · attacks **28 cases, 0 breaches, 0 ground-truth mismatches** · policy state after battery unchanged (18, all laws).
**Attack coverage:** A1 cross-account reads by id ×5 · A2 foreign business_id insert/update/delete ×3 (42501 + counts intact) · **A3 re-home blocked by WITH CHECK** (the case a USING-only UPDATE policy would leak) · A4 DELETE-by-member refused ×2 · A5 junction tampering ×4 (role still 'member', junction still 6 — escalation structurally impossible, no write policy exists) · **A6 ownerA cannot write his own account** (owner_user_id grants nothing — R-A proven directly) · A7 reference read-only ×2 · A8 deny-all tables logged with service-role row counts so `0` means refused not empty ×3 · A9 anon sweep ×5.
**R-C PROVEN (closes Proto 06 N-3):** signed in once, **access token asserted byte-identical** across the revocation; B1 rows → **0 immediately**, A1 intact at 200, store list narrowed; re-grant equally immediate; junction restored to 6. Operationally: offboarding lands on the next query, not the next token refresh — no stale PHI window.
**Evidence:** `evidence/X4-matrix_*.log`, `X4_scoping_*.log`, `X4_attacks_*.log`, `X4_revocation_*.log`
**Report:** `RESPONSES/response_2026-09-01_141500_bim002-X4-complete.md`
**STOPPED before X5.**

### [15:00] — X5 COMPLETE: one command, twice from empty, normalised diff IDENTICAL

**Rulings applied:** N-3 CLOSED (F-13 recorded for TRANSFERS: membership evaluated live, revocation AND re-grant land on the next query in-session) · A6 recorded as the R-A proof · fail-closed sign-in + identity assertion now standing harness law.
**Built:** `scripts/rls-harness/ac8-check.mjs` (AC8 as a permanent instrument, meaningful only after from-scratch apply per E-5) · `prove.mjs` (the one command) · npm task **`rls:prove`**.
**Pipeline:** wipe F-6 → bootstrap → chain **0001–0027** → seed → AC8-fresh → policy-check → matrix → scoping → attacks → revocation. Wipe/chain reuse BIM-001's certified `db-reset.mjs` (one wipe implementation, no drift).
**Run 1 (`X5_prove_…T0654.log`) and Run 2 (`…T0656.log`) both: ISOLATION PROVEN** — AC8 GREEN · 18 policies + ALL LAWS · 320/320 matrix · SCOPING EXACT · 28 attacks 0 breaches 0 ground-truth mismatches · R-C proven with byte-identical token.
**Diff:** raw = **4 changed lines only** (run banner ts, matrix log ts, one generated business_id in A3.1 ground truth, session token fingerprint). **Normalised twin (uuids/ts/ms/token masked, generated by the runner itself): IDENTICAL byte-for-byte.**
**AC8 from scratch:** all four helpers secdef/stable/search_path-pinned, ACL shows **no bare `=X/` (PUBLIC) and no `anon=X`**, anon denied at execution (42501) — both channels closed on a database that never saw a create-or-replace. Now inside the one command, so it can't be skipped.
**Report:** `RESPONSES/response_2026-09-01_150000_bim002-X5-complete.md`
### [15:30] — X6 COMPLETE: replica proves the same chain; scratch untouched

**Option (a) adopted by Director** — `RLS_REPLICA_*` added; no `RLS_HARNESS_*` exists; scratch stays on A-1 fallback.
**Recon before any wipe:** replica ref **`ihgcsrypblqkwommrkgj`** @ aws-1-ap-south-1 · scratch `jmzwhgnyunwssamrqyhp` @ aws-1-us-west-1 · both aws-1 ✓ · refs internally consistent ✓ · **distinct targets asserted** ✓. Replica pre-wipe held 16 tables (BIM-001 X2 leftovers).
**Bootstrap catalog gate (before the chain) — MATCHES DB_BASELINE.md:** tables exactly [profiles, user_roles] · 3 baseline policies byte-faithful · RLS on both · functions [handle_new_user, rls_auto_enable] with **update_updated_at correctly ABSENT pre-chain** (E-1) · ensure_rls present · app_role enum · on_auth_user_created. Evidence `evidence/X6_bootstrap_catalog_match.log`.
**Pipeline (one run):** ISOLATION PROVEN — AC8 GREEN · 18 policies + ALL LAWS · 320/320 · SCOPING EXACT · 28 attacks 0 breaches 0 GT mismatches · R-C proven. Seed purged **14** accumulated auth identities (FK-safe reset held on a dirtier target).
**Normalised diff vs X5 run 2: 3 differences, ALL target properties, ZERO behavioural** — pooler host (proves it ran on the replica), 7× `[env] A-1 fallback` banner absent on replica (prefix mechanism working), `purged 4`→`purged 14`. Everything else byte-identical. **Did NOT retro-fit the normaliser to hide the host** — masking target identity would make a cross-target diff look clean while destroying the line that proves which DB ran.
**Scratch untouched confirmed:** policies=18, tables=16, user_data=600.
**Flagged, not silently fixed:** `prove.mjs` hardcodes label `X5-matrix`, so X6 sub-logs are named `X5-*_T0725` — unambiguous by timestamp and correctly referenced inside the X6 consolidated log; renaming would break those references. Fix at X7 = phase-label argument.
**Report:** `RESPONSES/response_2026-09-01_153000_bim002-X6-complete.md`
**STOPPED before X7.**

---
_(superseded X5 stop-note below, kept for the record)_
**Previously STOPPED AT X6.** Requested env lines (names only) + flagged a **silent-retarget hazard**: adding `RLS_HARNESS_*` makes every `rls:prove` retarget to it, including scratch runs. Recommended option (a): name the replica set `RLS_REPLICA_*` and run X6 as `RLS_HARNESS_PREFIX=RLS_REPLICA_` (prefix switch already built into loadEnv). Replica must be pre-loaded with the 2-table/3-policy baseline.

### [16:15] — X7 + CLOSE-OUT COMPLETE — ENGINEERING DONE

**X7 board/fences all green:** build 22 routes · tsc clean · jest **28/128/0** (= BIM-001 baseline) · **`git diff src/types/supabase.ts` vs certified `9f8c80d` EMPTY** · Gap-6 grep over 0016–0027 (comments stripped) zero hits · `storage.objects` policies 0 before AND after on both targets · **zero `src/**` writes** · AC19: BIM-002 introduced exactly ONE service-role site (`rls-harness/lib/db.mjs`), app-side ones inherited and untouched.
**Harness improvements shipped (both approved):** `prove.mjs` phase-label arg (X6 sub-logs left intact with their references) · `--compare-behaviour` (strips env banners + identity counts, **never** the host).
**Close-out artifacts:** `AUTHORITY/RLS_TEMPLATES.md` (4 helpers E-4 form, T-1 C-form, T-2/3/4/5, R-A, deny-all, **B as anti-pattern with shield table**, 6 structural laws) · `ACTIONS/PROTO06/TRANSFERS_ADDENDUM_BIM-002.md` (**F-10..F-13**, TRANSFERS/FINDINGS untouched) · ACCEPTANCE_SPEC **ENGINEER EVIDENCE-FILLED** AC1–AC20 with paths, **AC13 STRUCK**, prose re-read vs E-1..E-5 · RETROSPECTIVE · manager §10a **CF-1..CF-7**.
**Handoff:** `RESPONSES/response_2026-09-01_161500_bim002-X7-handoff.md` — 4 per-concern commit blocks, prepared never run.
**ENGINEERING COMPLETE. STOPPED.**

## Lessons Learned

- **Every defect this module found was in an instrument or the inherited template — never in a policy.** Eleven migrations landed red→green first try and held across three from-scratch proofs on two databases.
- `create or replace` preserves ACLs, so privilege ACs are only honest after a drop-and-apply (F-12) — X1's green was an artifact and X5 would have failed.
- A performance measurement must assert correctness parity first: formulation B looked 160× faster while returning zero rows (F-11).
- Refusing to normalise away the pooler host kept X6's diff honest; a clean diff you can't trust is worth less than three explained lines.
- Fourth instrument defect of the campaign was my own ad-hoc script treating a failed sign-in as "0 rows" — fail-closed-on-auth is now standing harness law.

### [session close] — HANDED TO QA, ENGINEER STANDING BY

Director takes BIM-002 to QA. Engineering holds here; **no further work in this lane until the QA report returns** (expected next session).

**State at hand-off:** all gates X0–X7 green · working tree carries the full uncommitted BIM-002 batch (nothing to exclude) · both throwaways hold post-chain state for replay · dev backend untouched.

**What QA needs, and where it is:**
- Finalized spec with per-AC evidence paths → `ACTIONS/BIM-002-CYBER-PHARMA/ACCEPTANCE_SPEC.md`
- Evidence root (60+ logs, unique filenames) → `ACTIONS/BIM-002-CYBER-PHARMA/evidence/`
- Errata E-1…E-5 → `ACTIONS/BIM-002-CYBER-PHARMA/ERRATUM.md` (read before judging AC8, AC9, AC13)
- Independent replay: `npm run rls:prove` (scratch) · `RLS_HARNESS_PREFIX=RLS_REPLICA_ npm run rls:prove` (replica). One command, exits non-zero on any failure.
- `QA/` folder present and empty, ready for Sol's package.

**On return, the close-out sequence is:** file the Gate Q report in `QA/` → spec lifecycle → QA-VERIFIED with certified SHA pinned → manager → CLOSED with deliverables map → CHANGELOG + RECOVERY + session log → tombstone, and route CF-1…CF-6 onward.

## End of Session State

- **Working:** Everything. BIM-002 engineering complete, all gates X0–X7 green, board at 28/128/0.
- **Broken:** Nothing. Dev backend untouched (still 2-table baseline, per LIVE APPLY DEFERRED).
- **Next Steps:** Director — 4 per-concern commits → SHA pin → PRE-Q → Sol's Gate Q. Engineer resumes on the QA report for close-out. Then CF-1..CF-6 (notably credential rotation on 4 throwaways, and `proto-06/` + `phase-3-proto-6` deletion now the port is certified).

## Files Changed This Session

- `supabase/migrations/0016–0027` — 4 helpers + 15 policies across 11 tables
- `scripts/rls-harness/` — 13 files (libs, expectations, payloads, seed, harness, scoping, attacks, revocation, policy-check, ac8-check, x3 driver, prove) + `package.json` task `rls:prove`
- `agent_docs/AUTHORITY/RLS_TEMPLATES.md` · `agent_docs/ACTIONS/PROTO06/TRANSFERS_ADDENDUM_BIM-002.md`
- `agent_docs/ACTIONS/BIM-002-CYBER-PHARMA/` — ACCEPTANCE_SPEC (evidence-filled), CLAUDE.md (§10a carried flags), ERRATUM.md (E-1..E-5), RETROSPECTIVE.md, evidence/ (60+ logs)
- `agent_docs/RESPONSES/` — 8 BIM-002 artifacts · this session log · `RECOVERY.md`
