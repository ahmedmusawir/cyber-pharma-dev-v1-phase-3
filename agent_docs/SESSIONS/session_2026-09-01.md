# Session Log: 2026-09-01

## Project Context

- **Project:** CYBER_PHARMA — cyber-pharma-dev-v1-phase-3
- **Tool:** Claude Code
- **Goal:** BIM-002-CYBER-PHARMA (RLS policies) — module staged; awaiting the Operator launch line. No task assigned yet.

## Starting State

- **Branch:** `phase-3-bim002` @ `aca3d05` ("1sep2026 - bim002 has begun") — working tree CLEAN.
- **Last Working Feature:** BIM-001 CLOSED (Gate Q PASS, certified `9f8c80d`, zero rework) — sixteen-table schema chain, tooling, regenerated types. Board at close: build 22 routes · tsc clean · jest 28/128/0.
- **Campaign board:** BIM-000 ✅ · FIX-001 ✅ · BIM-001 ✅ · PROTO 06 rig CLOSED (transfers consumed) → **BIM-002 next**.

### New since last session (Director/Architect staging, all committed)

- `agent_docs/ACTIONS/BIM-002-CYBER-PHARMA/` — manager, ACCEPTANCE_SPEC, own AUTHORITY/, evidence/, QA/ (unread — Plan Mode reads them at the launch line).
- **`agent_docs/AUTHORITY/` promoted to campaign level** (doctrine sync `3eba19f`: BIM_PLAYBOOK + CAMPAIGN_MAP v1.1 + README).
- **PROTO 06 artifacts copied forward** to `agent_docs/ACTIONS/PROTO06/`: `TRANSFERS.md` + `FINDINGS.md` alongside the brief and plan. The rig's policies/harness are on main; the `phase-3-proto-6` branch is slated for deletion after BIM-002's harness re-point.

### Known issues / carries into this module

- **KIP-1 — still OPEN/parked** (`server.ts` deprecated cookie adapter + `as any`). FORCED-ENTRY trigger is "touching `src/utils/supabase/server.ts`" — BIM-002 is policy work, so the trigger is **not expected to fire**; will surface immediately if scope drifts into that file. KIP-2 CLOSED 2026-08-27.
- **From PROTO 06 (TRANSFERS §5), owed to BIM-002:** live-session junction revocation (N-3) · volume beyond 6k rows (N-4) · multi-policy fan-out across sixteen tables (N-5) · **`accounts`-level access — no policy pattern exists yet (N-6), the likeliest place BIM-002 needs new design**. Storage leg (N-1) and browser-client leg (N-2) are owed elsewhere, not here.
- **From PROTO 06 findings, binding on this module:** F-1 land SELECT before any write policy on every tenant table · F-8 policy order helpers → SELECTs → writes · F-5 SECURITY DEFINER helper verbatim · F-2 PostgREST 1,000-row cap in any verification · one permissive policy per operation per table.
- Older carries: numbered-color predicate rebuild · QA-FINDING-001 (dark-mode login contrast) · `report_files` fidelity flag · ERRATUM E-2 (DATA_CONTRACT §3 amendment, Director doc-repo staging).
- **Director post-BIM-001 duties status unknown to me:** LIVE apply of the migration chain · E-2 staging · throwaway-credential rotation. Worth confirming before BIM-002 assumes live schema state.

## Session Progress

### [morning] — SESSION OPENED

**Task:** Step-0 protocol — RECOVERY.md read, branch/commits verified (clean at `aca3d05`), KIP registry surfaced (KIP-1 parked, trigger not met), PROTO 06 transfer obligations carried forward, session file created.
**Status:** Ready. Awaiting the BIM-002 launch line → Plan Mode.

### [10:22] — PENDING_APPROVAL — BIM-002 launch plan

**Task:** BIM-002-CYBER-PHARMA (RLS policy campaign). Launch line received; authority read in precedence order; Amendment A-1 honored (PROTO06_DB_URL for X0–X5, RLS_HARNESS_* rename at the port, .env.local never edited).
**X0 EXECUTED AND GREEN** (`evidence/X0_catalog_2026-09-01.log`): scratch wiped in F-6 order → chain 0001–0015 applied → **16 tables, all rowsecurity=true, exactly 3 policies (baseline only), 0 on the fourteen new**; `ensure_rls` present; `storage.objects` policies = 0 (AC11 before-state banked); pooler `aws-1` confirmed; all tenant-key columns verified; **all T-7 indexes already present — no index migration needed**.
**Plan:** `agent_docs/RESPONSES/response_2026-09-01_102200_bim002-launch-plan.md` — 0016 helpers + 0017–0027 (one file per table, 15 new policies), harness port map to `scripts/rls-harness/`, R-B A/B method with 1-hour box and tie-break rule, X1–X7 sequence, 4 per-concern commit groups.
**FLAGS (9):** ⛔1 `apa_memberships` has NO tenant key (only nullable soft-ref `discount_redeemed_business_id`) → recommend deny-all · ⛔2 manager's "17 policies" doesn't reconcile with its own breakdown → X0-confirmed count is **15** · ⛔3 **`proto-06/scripts/` NOT copied forward** — the 4 copied harness files have broken imports and the seeder/one-per-op-checker/one-command-proof are missing; need the copy (I may not check out that branch) · ⛔4 no CI config exists anywhere → propose `.github/workflows/rls-harness.yml`, confirm GitHub Actions · ⛔5 X6 replica not provisioned (BIM-001's `ihg…` replica is a reuse candidate) · 6 `user_data` DELETE=admin is strikeable, building as ruled · 7 carried non-decisions (businesses INSERT/DELETE, subscriptions Phase 7, report_files fidelity) · 8 `AUTHORITY/README.md` vs `AUTHORITY_README.md` on disk · 9 scratch's `rls_auto_enable`/`update_updated_at` are bootstrap reproductions, not the AC8 target.
**Status:** Awaiting approval. No migration or harness file authored.

## Lessons Learned

- (pending)

## End of Session State

- (pending)

## Files Changed This Session

- `agent_docs/SESSIONS/session_2026-09-01.md` — created
- `RECOVERY.md` — pointer refreshed to this branch/session
