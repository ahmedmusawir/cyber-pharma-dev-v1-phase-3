# QA — BIM-002-CYBER-PHARMA

Module-scoped QA home (Phase 3 doctrine). Born empty at authoring.

**Lifecycle:** Engineer handoff → PRE-Q on working tree (Sol designs, Cody executes, Director as hands) → One-Walk → per-concern commits → SHA pin → certification.

**Expected contents at Gate Q:**
- `GATE_Q_REPORT_BIM-002-CYBER-PHARMA_<VERDICT>.md` (Sol)
- Cody execution report + runners + per-AC logs
- Sol's QA-logged journal entry (two-seat rule — returned WITH the FINAL_PASS report, not after)

**Intake (QA_INTAKE_CHECKLIST):** finalized ACCEPTANCE_SPEC.md · engineering handoff · `evidence/` · `agent_docs/ACTIONS/PROTO06/TRANSFERS.md` §2 (harness contract) · scratch + clean-replica pooler notes (`aws-1-us-west-1`, session pooler, IPv4).

**Standing instrument rules:** `pg_catalog` only · repo-root-anchored runners · unique evidence filenames · count-based volume assertions · mutation denials ground-truthed.
