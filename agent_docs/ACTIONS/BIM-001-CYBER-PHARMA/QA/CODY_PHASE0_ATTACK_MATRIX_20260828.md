# CODY QA EVIDENCE — BIM-001 Phase 0 Preflight and Attack Matrix

- Probe identity: `CODY-BIM001-PHASE0`
- QA role: Cody, independent execution agent under Sol; no Gate Q adjudication
- Timestamp: 2026-08-28 (Asia/Kuala_Lumpur)
- Repository: `cyber-pharma-dev-v1-phase-3`
- Branch: `qa/bim-001-cody-01`
- Tested HEAD: `fefde109fe50eb55839dee4dd29129b2ea3de90c`
- Target class: local repository, read-only inspection
- Working-tree state at preflight: clean (`git status --short` emitted no entries)
- Database work performed: none

## Phase 0 result

Repository and branch identities match the engagement. The finalized Acceptance Spec, module manager, Engineering handoff, authority documents, migrations `0001` through `0015`, reset/bootstrap/verification tooling, and generated Supabase types are present. Engineering implementation exists at the tested HEAD.

The local shell does not currently expose `DB_URL` or `DB_RESET_ALLOW`. `.env.local` contains application Supabase key names, but target URLs/credentials were not read or printed. Before any database execution, scratch and replica identities must be independently established as throwaway QA projects and the required connection material must be supplied without entering evidence.

## Independent attack matrix

| AC | Independent QA attack | Execution method | Expected observation under the Acceptance Spec | Planned Cody evidence artifact |
|---|---|---|---|---|
| AC1 | Attack migration 0001 on an empty database, then separately catalog the frozen baseline before applying it. Verify the named loud failure and verify 0001 does not structurally mutate `profiles` or `user_roles`. | Throwaway replica; direct `psql`/`pg` execution of 0001 against an empty schema; `pg_catalog` snapshots and structural hashes before/after baseline-chain replay. | Empty target aborts non-zero with `BIM-001/0001 BASELINE ASSERT FAILED`; documented bootstrap is the only zero path; baseline tables/policies/functions remain contract-faithful. | `CODY_AC01_BASELINE_NEGATIVE_<run-id>.log`, `CODY_AC01_BASELINE_DIFF_<run-id>.log` |
| AC2 | Rebuild from the documented zero/bootstrap path while recording every migration in resolved lexical order and checking for unrepresented prerequisites. | Proven scratch only; `DB_RESET_ALLOW=yes npm run db:reset`; independent catalog dump afterward. | Exit 0; all 15 migrations apply in dependency order; exact target inventory materializes without hidden state. | `CODY_AC02_ZERO_BOOTSTRAP_<run-id>.log` |
| AC3 | Construct the frozen two-table baseline, snapshot its objects, apply only the migration chain, and compare baseline objects before/after. | Proven replica only; baseline bootstrap followed by `npm run db:apply`; `pg_catalog` object definitions and policy/function counts. | Exit 0 with no duplicate-object errors; baseline structure/policies preserved; final exact inventory is produced. | `CODY_AC03_BASELINE_REPLAY_<run-id>.log`, `CODY_AC03_BASELINE_OBJECT_DIFF_<run-id>.log` |
| AC4 | Compare actual table names as sets, not counts; separately assert deferred names are absent. Inventory PK, FK, and UNIQUE constraints for every required table. | Read-only `pg_catalog` SQL on both scratch and replica. | Exactly the named sixteen public base tables; no deferred tables; declared PK/FK/UNIQUE constraints present. | `CODY_AC04_INVENTORY_CONSTRAINTS_<run-id>.log` |
| AC5 | Catalog the account spine and attempt orphan/null relationship inserts inside rollback-only transactions. | `pg_constraint`/`pg_attribute`; negative inserts on proven scratch. | `businesses.account_id` is NOT NULL FK to accounts; `subscriptions.account_id` FK to accounts and no `business_id`; `accounts.owner_user_id` FK to `auth.users`; invalid relationships rejected. | `CODY_AC05_ACCOUNT_SPINE_<run-id>.log` |
| AC6 | Enumerate the role column type/CHECK and try `member`, `admin`, `owner`, case variants, and NULL with otherwise valid fixture rows. | Catalog plus transaction-scoped mutation probe on proven scratch. | Exactly `admin` and `member` accepted; third values and NULL rejected by the database. | `CODY_AC06_ROLE_NEGATIVE_<run-id>.log` |
| AC7 | Inventory RLS and policies, compute policy delta from the frozen baseline, then execute SELECT independently under `anon` and non-service `authenticated` roles on every one of the fourteen new tables. | `pg_class`, `pg_policy`/`pg_policies`, and `SET LOCAL ROLE` probes; no service-role-success inference. | RLS enabled on all new tables; zero new policies; exactly baseline policy state preserved; neither test role reads rows from any new table. | `CODY_AC07_DENY_DEFAULT_<run-id>.log` |
| AC8 | Discover monetary columns from the actual schema and authority-declared list, then reject all float-family storage. | `pg_attribute`/`pg_type` plus authority-to-catalog comparison. | Every declared monetary column is NUMERIC; no monetary value uses real/float/double precision. | `CODY_AC08_MONEY_TYPES_<run-id>.log` |
| AC9 | Discover every occurrence of `ndc`, `drug_ndc`, `script`, `bin`, `pcn`, and `group_field`; compare actual base types. | Catalog query over all public columns. | Every occurrence is text/character varying and none is numeric. | `CODY_AC09_IDENTIFIER_TYPES_<run-id>.log` |
| AC10 | Extract the CHECK definition and attack with all ratified values, an out-of-vocabulary value, empty string, case variants, and NULL. | Catalog plus rollback-only inserts on proven scratch. | Ratified vocabulary accepted; out-of-vocabulary values rejected; NULL behavior reported exactly as implemented/ratified. | `CODY_AC10_MEDICAID_VOCAB_<run-id>.log` |
| AC11 | Independently inventory provenance columns, FK targets, nullability/types, and dataset identity/checksum/row-count structure. | `pg_catalog` structural query for the four reference tables and `reference_dataset_versions`. | Each reference table has source file, imported-at, and dataset-version linkage; registry carries checksum, row count, and unique per-dataset identity. | `CODY_AC11_PROVENANCE_<run-id>.log` |
| AC12 | Inspect all sixteen tables without excluding the baseline pair; inventory both timestamp columns and trigger definitions. Where possible, update a disposable row per structurally eligible table and observe `updated_at`. | `pg_attribute`, `pg_trigger`, `pg_get_triggerdef`; transaction-scoped or cleaned fixtures on proven scratch. | Literal AC12 expectation: all sixteen have `created_at`/`updated_at` TIMESTAMPTZ, an `update_updated_at()` trigger, and updates bump `updated_at`. Any baseline exception is reported as `CONTRACT/EVIDENCE TENSION — AC12`, not adjudicated. | `CODY_AC12_ALL16_TIMESTAMPS_<run-id>.log` |
| AC13 | Execute the documented reset twice on scratch, capture exit codes and canonical post-run inventories, and byte-compare the canonical inventories. Do not represent QA execution as the Director witness. | Proven scratch only; exact one-command reset twice; sorted catalog snapshots and digest/diff. Director separately performs the One-Walk manual point. | Both deterministic runs exit 0 and inventories are identical. Director-witnessed execution remains a distinct required observation. | `CODY_AC13_RESET_RUN1_<run-id>.log`, `CODY_AC13_RESET_RUN2_<run-id>.log`, `CODY_AC13_INVENTORY_DIFF_<run-id>.log` |
| AC14 | Compare generated TypeScript table names and relation shapes against the post-chain catalog; assert deferred types absent; run TypeScript. | Local parser/static comparison plus `npm` TypeScript command. | Sixteen expected table types present; deferred types absent; no obvious catalog/type drift; tsc exit 0. | `CODY_AC14_TYPES_CATALOG_DIFF_<run-id>.log`, `CODY_REGRESSION_TSC_<run-id>.log` |
| AC15 | Run TypeScript, full Jest, and production build independently and record actual counts/routes. Inspect git diff scope for auth-flow implementation changes relevant to regression risk. | Non-destructive local `npm` regression board; build route capture; read-only git diff/log inspection. | Build succeeds with 22 routes; TypeScript clean; Jest at least 28 suites/128 tests/0 failures; no BIM-001 auth-flow regression observed. | `CODY_REGRESSION_BUILD_<run-id>.log`, `CODY_REGRESSION_JEST_<run-id>.log`, `CODY_REGRESSION_TSC_<run-id>.log` |

## Contract questions/tensions identified during static inspection

1. **CONTRACT/EVIDENCE TENSION — AC12.** AC12 and manager structural law §6.5 say all sixteen tables carry both timestamps and the update trigger. The baseline authority defines `user_roles` and `profiles` with `created_at` only, while module scope forbids structural changes to those tables. Migration 0001 does not retrofit them, and Engineering evidence explicitly checked only the fourteen new tables. QA will report the all-sixteen catalog result literally and leave the ruling to Sol/Director.
2. **CONTRACT QUESTION — AC3 baseline function count.** AC3 describes the baseline replica as having three functions. ERRATUM E-1 and migration 0001 say the live/frozen pre-chain baseline has only `handle_new_user()` and `rls_auto_enable()`, with `update_updated_at()` created by 0001. QA will capture pre/post function inventories and will not choose between these formulations.
3. **Target proof outstanding.** The handoff identifies scratch and replica classes and pooler regions, but the present shell exposes no `DB_URL`, and no target identity has yet been printed or independently proven throwaway. No destructive or database command is authorized until that is resolved.

## Phase 0 execution boundary

No reset, bootstrap, migration, database connection, mutation probe, TypeScript, Jest, or build command was executed during Phase 0.
