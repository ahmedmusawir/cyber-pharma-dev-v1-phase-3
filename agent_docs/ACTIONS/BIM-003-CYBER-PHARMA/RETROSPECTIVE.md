# RETROSPECTIVE — BIM-003-CYBER-PHARMA · Audit Machinery

**Engineer close, 2026-09-14.** Three stages, one day, zero migration errors, one mid-stage self-correction (S2: `owedbook_rows` logged after a COUNT; the body-shape check caught it against Brief §5 and it was fixed before the stage closed). Not self-certified — QA decides.

## Keep

- **Read the whole chain before planning.** TV-1 turned up that `audit_logs` already existed (0015) and that "sixteen" included the trail itself — two of the nine red flags came from reading files, not the pack, and both became errata before build instead of rework after it.
- **Assert-then-create on every migration, including the drop.** `0028` refuses to drop a non-empty trail; the stamps refuse a missing function or a duplicate trigger. The chain fails loudly on the wrong target.
- **Catalog evidence generated by a script, writes rolled back.** `audit-catalog.mjs` re-ran green at S2 for S1's assertions in seconds; QA can run it too.
- **Ground-truth every audit-row claim through the service role**, and assert the identity each session runs as. "0 rows" is never accepted as proof.
- **Symbolic golden with a watermark (E-2).** A committed, readable expected trail that survives regenerated uuids.
- **Log-then-read as a mechanical check, not a code-review note.** The body-shape assertion (membership < raise < insert < read, exactly one insert) is what caught the S2 defect.

## Change

- **The pack's "sixteen" should be derived, not fixed at write time.** Any count that the chain can answer should be a query in the spec's method column, with the number recorded as evidence.
- **Stage evidence generators should be written before the migrations**, so the first from-scratch apply is already graded. S1 wrote them after.
- **Wrapper mapping (A-3) should be an Architect ruling in the pack, not an Engineer assumption approved at Plan Mode.** Four NULL fields and one column alias (`pbm ← insurance`) are product decisions.
- **Name the target throwaway and its env block in the manager**, not only in ENV_NOTE — the plan had to ask.

## Drop

- **Hardcoded evidence paths in shared harness sub-runners.** `rls:prove`'s stages write into BIM-002's certified folder; S1 had to move seven files by hand to respect AC-901. (CF-8 addition, BIM-005.)
- **Duplicating the actor-role expression in five function bodies.** A tiny `audit_actor_role()` helper would remove four copies; not done here because it was outside the approved plan. Candidate for the next audit touch.

## Deferred Ledger candidates (routed, not built)

| Candidate | Why it surfaced | Owner |
|---|---|---|
| Redaction inside `old_data`/`new_data` (the trail is PHI) | Brief §3 "not in v1" | Phase 8 |
| Request / correlation id on audit rows | Brief §3 | Phase 8 |
| Audit viewer UI (tenant-facing) | R-1 keeps the viewer out of scope | later phase |
| MissionControl inbox reader over `audit_logs` | R-5 | MissionControl |
| Retention / partition mechanism (six-year minimum documented, `occurred_at` indexed) | R-3 | Phase 8 |
| `audit_actor_role()` helper to de-duplicate the JWT-role expression | five copies today | next audit touch |
| `db-verify.mjs` AC7 re-baseline (asserts zero non-baseline policies; red since 0016) | CF-9 | QA Cleanup |
| Harness evidence path by module + `prove.mjs` "18 policies" verdict text + real env prefix for scratch | CF-8 additions | BIM-005 |
| Federal tab / `aac`, `federal_expected`, `federal_diff`, `report_file` population (NULL by A-3) | wrappers mirror the shape, not the math | Phase 5 / Phase 6 |
| `pending_registrations` rows with NULL `business_id` log NULL + NULL context (row_id still carries the key) | R-8 split at TV-2 | note for BIM-004 onboarding |
| Direct member SELECT on `user_data` is unaudited at the DB layer (read audit = wrapper reads only) | QA D-1 / CF-10 — BIM-002's certified member SELECT policy vs Brief §1 prose (SOL Gate Q 2026-09-15) | BIM-005 (app-layer routing) · permissions-v2 (DB-layer closure, if ever) |

🥄 *The camera did not blink. Whether it saw everything is QA's call.*
