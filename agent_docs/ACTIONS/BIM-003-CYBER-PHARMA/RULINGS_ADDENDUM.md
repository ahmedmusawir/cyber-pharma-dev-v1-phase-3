# RULINGS_ADDENDUM — BIM-003-CYBER-PHARMA (append-only; CLAUDE.md §5 and BIM003_BRIEF.md are frozen)

- R-6a (2026-09-14, Architect, credited Claudy): BEFORE TRUNCATE FOR EACH STATEMENT trigger on audit_logs raising unconditionally. Row triggers never see TRUNCATE; without it the service role could empty the trail in one statement. Extends R-6 "every role, no exceptions."
- R-8a (2026-09-14, Architect): businesses is stamped with business_id = its own id (its id IS the business). Precedent 0019:25.
- R-10 (2026-09-14, Architect): the S3 audit runner seeds a small set of user_data rows with insurance, new_paid, and status populated so the golden trail exercises summary, pbm_options, and the updated tab non-empty. Audit-runner seed only; BIM-002 cast untouched.
- CF-9 (carried flag): scripts/db-verify.mjs AC7 asserts zero non-baseline policies and has been red since BIM-002 (0016). Pre-existing. Route: QA Cleanup — re-baseline AC7 to the certified policy set. Sol informed via this file.
- CF-8 additions (harness improvements → BIM-005): rls-prove sub-runners write evidence to a hardcoded BIM-002 path; prove.mjs verdict line says "18 policies" (now 19); give scratch a real env prefix so the harness never depends on the app's NEXT_PUBLIC_* runtime names.
- CF-10 (carried flag, QA D-1, SOL Gate Q 2026-09-15 — contract/threat-model gap, NOT a BIM-003 defect): an authorized junction member can SELECT user_data directly via PostgREST (BIM-002's certified user_data_select_member, 0021:28-30) and receive patient-shaped rows with zero read_page audit rows; the owedbook_* wrappers audit correctly (+1 per call, QA Stage C). Brief §1 "every read of patient-shaped data … leaves a row" is stronger than the frozen architecture guarantees: the DB layer audits wrapper reads only. Routes: (1) Architect — doctrine/campaign-map note that read audit is wrapper-scoped at the DB layer; (2) BIM-005 — the service swap must route every OwedBook read through the wrappers (the app-layer guarantee); (3) Deferred Ledger — closing direct member SELECT on user_data at the DB layer is a permissions-v2 / later-phase decision, not this module's. Evidence: QA/evidence/QA_C_probes_2026-09-15T0835.log · QA/CODY_QA_RECON.md §7 D-1.
- CF-8 QA corroboration (SOL Gate Q 2026-09-15): audit-prove.mjs likewise hard-writes to ACTIONS/BIM-003-CYBER-PHARMA/evidence/ (the CF-8 addition above names only rls-prove → BIM-002's path); QA relocated runner output 12× during Stages A–I. Fix in BIM-005 = evidence root by module + env override.
- CF-10 (2026-09-15, Sol D-1, ratified Architect): BIM-002's direct member SELECT on user_data bypasses read-audit. Not a BIM-003 defect; the contract scoped read-audit to the wrappers. Route: BIM-005 sends every app read through owedbook_* wrappers; closing direct SELECT at the DB layer is a permissions-architecture ruling for a later module. "Every PHI read is logged" is not true until both land. _(Ratified text of record, appended at closeout 2026-09-16; supersedes the 2026-09-15 draft CF-10 line above, which stays for the audit trail.)_
- E-8 note: supabase/.temp/cli-latest version string changed by Director types regen; recorded by Sol in the erratum lane; generator artifact.
