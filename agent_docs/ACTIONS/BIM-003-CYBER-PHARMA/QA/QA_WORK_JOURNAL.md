# QA_WORK_JOURNAL — BIM-003-CYBER-PHARMA · QA lane (append-only, per J-13)

| # | When | Path | Purpose | QA question answered | Durable / helper | Env mutation | Restoration |
|---|------|------|---------|---------------------|------------------|--------------|-------------|
| 1 | 2026-09-15 (Cody recon) | `QA/CODY_QA_RECON.md` | Independent static recon report for SOL: specimen pin, contract read, claim map, implementation delta, static attack surface, proof-instrument review, discrepancies, proposed dynamic attacks | A–H of the recon prompt | **Durable evidence** | none | n/a |
| 2 | 2026-09-15 (Cody recon) | `QA/helpers/qa-reset.mjs` · `QA/helpers/qa-golden-attack.mjs` · `QA/helpers/qa-probes.mjs` | Stage A/B/C–G drivers staged for the dynamic campaign — see RECOVERY.md handoff block for purposes | none (prep only) | **Helper** (staged, never executed — no certification evidence) | none — golden corrupted only inside qa-golden-attack with byte-exact restore | n/a (no run) |
| 3 | 2026-09-15 (session handoff) | `RECOVERY.md` + this journal | SESSION HANDOFF ONLY (Cody Prompt #3): campaign staged, **Stage A NOT executed — SCRATCH not reset or mutated by QA, golden byte-untouched, no dynamic QA result exists.** Stop reason: Claude Code Auto safety-classifier outage (glm-5.3-flash:cloud timeouts), infrastructure only. Next Cody re-pins, then runs `node QA/helpers/qa-reset.mjs` → `npm run rls:prove` → `npm run audit:prove` → Stages B–I per SOL Prompt #2 | none (handoff only) | Bookkeeping | none | n/a |
| 4 | 2026-09-15 14:36–14:43 (Cody, SOL Stage A release) | `QA/evidence/QA_A_db_reset.log` · `QA/rls-prove/X5_prove_2026-09-15T0638.log` (+6 siblings) · `QA/evidence/S3_audit_prove_2026-09-15T0642.log` (+3 siblings) | **STAGE A EXECUTED.** Re-pin clean (`qa/phase-3-bim003` @ `c45949e`) → `qa-reset.mjs` exit 0 (47/47 migrations, 16 tables, no warnings) → `rls:prove` exit 0 (ISOLATION PROVEN: 19 policies, 320 cells, scoping, 28 attacks / 0 breaches, live-session revocation R-C) → `audit:prove` exit 0 (TRAIL PROVEN: immutability 6 probes, write trail, R-8, 6 wrapper reads +6 read_page, cross-tenant deny, admin-only visibility, **golden 10/10 exact**). Fresh evidence moved from Engineering folders (BIM-002 evidence ×7, BIM-003 evidence ×4) into QA lanes. Twin comparison: QA fresh `.normalised.log` vs Engineering committed S3 twin differs **only in the timestamp line** (same host) — reproducibility corroborated. | Reproducibility of Engineering's S3 claims on a fresh reset; BIM-002 regression baseline holds; BIM-003 golden matches from scratch | **Durable evidence** | SCRATCH reset + replayed ×2 more by rls:prove/audit:prove stage 1 (all authorized destructive-on-SCRATCH). Seed maps re-generated UUIDs by design (see journal note below). **REPLICA untouched** (no prefixed env used). Golden untouched. | n/a — SCRATCH is disposable; seed maps left as live state (Stage C–G helpers identity-assert against them) |

**Note (row 4):** `scripts/rls-harness/seed-map.json` + `audit-seed-map.json` show as `M` after Stage A — the seeder recreates rig identities each run and writes fresh UUIDs back (`lib/env.mjs:52` "Rig identities are recreated by seed.mjs every run"). This is harness by-design runtime state, NOT product drift; committed values were Engineering's 2026-09-14 run. Left in place intentionally: qa-probes.mjs reads these maps live for fail-closed identity asserts. SOL adjudicates whether to commit the QA-run values or restore at campaign end.

| 5 | 2026-09-15 14:50–15:28 (Cody, SOL Stage B release) | `QA/evidence/QA_B_golden_attack_<case>.log` + `_full.log` ×5 cases · `golden_committed_copy.json` · 5 corrupted-run `S3_audit_prove/session_*0714…0726` sets (relocated to `QA/evidence/`) | **STAGE B EXECUTED — GOLDEN-ORACLE ATTACK BATTERY.** Five corruption cases against the committed golden, each running canonical `audit:prove`: `action` (row 1 insert→delete) exit 8 DIFF@1 · `business` (A1→B1) exit 8 DIFF@1 · `remove-row` (golden 9 rows) exit 8 row-count FAIL 10v9 + 8 diffs · `add-row` (golden 11) exit 8 row-count FAIL 10v11 + DIFF@11 · `payload` (row 2 old_data→null) exit 8 DIFF@2. **All five: non-zero, mismatch detected, diagnosable from evidence, golden restored byte-exact before next case.** Post-battery: `status` → IDENTICAL; SHA before/after both `c30bfcddf88b164d912f656369edd73e7b0eb79b512f58bc819eb095894ccd4f`; `git diff` golden empty. **No false-green.** | Does the golden oracle catch every corruption class (G-1/G-2 residual-risk confidence, recon attack #2)? **YES — 5/5** | **Durable evidence** | Each corrupted case reset+replayed SCRATCH via audit:prove stage 1 (authorized). Golden transiently corrupted 1 case at a time in-memory of the run; never left modified. REPLICA untouched. Seed maps regenerated again per run (same row-4 note). | n/a — golden byte-identical to commit at close |
| 6 | 2026-09-15 ~15:35 (Cody, instrument repair before Stage C) | `QA/helpers/qa-probes.mjs` (2 lines) | **HELPER DEFECT #3 FOUND + FIXED (instrument only, zero product impact).** First Stage C run crashed `TypeError: S.member-A.from is not a function` — helper stored clients as `{c, uid}` in `S[sym]` but all 13 call sites invoked methods on `S[sym]` directly (missing `.c`). Fix: store the client itself (`S[sym] = await signIn(...)` in `mkIdentity`, line 37; same for `S.multi`, line 132). `.uid` field never read; signOut loop expects client shape — both now correct. Crash happened post-sign-in, pre-mutation → no destructive side effect. | Can the probe instrument run at all? **Yes after fix** | Helper | none (probe crashed before any mutation) | n/a |
| 7 | 2026-09-15 16:35 (Cody, SOL Stages C–I autonomous release) | `QA/evidence/QA_C_probes_2026-09-15T0835.log` | **STAGE C EXECUTED — D-1 DYNAMICALLY CONFIRMED (exit 0).** member-A=staffA (junction MEMBER of A1 only). DIRECT `user_data` SELECT: **ALLOWED, 204 rows = svc ground truth 204, every row's business_id ∈ staffA's set (no leakage), +0 read_page**. WRAPPER `owedbook_kpis(A1)`: **ALLOWED, +1 read_page, actor_user_id=staffA, actor_role=authenticated**. Verdict: DIRECT=ALLOWED(204, +0 audit) vs WRAPPER=ALLOWED(+1 audit). | Does a direct authorized member table read succeed WITHOUT read auditing while the wrapper read IS audited? **YES — D-1 confirmed live.** Classification stays provisional CONTRACT-SPEC/THREAT-MODEL per SOL (implementation matches frozen contract; Brief §1 mission prose overstates) | **Durable evidence** | none (reads + one audited wrapper call only) | n/a |
| 8 | 2026-09-15 16:38 (Cody) | `QA/evidence/QA_D_probes_2026-09-15T0838.log` | **STAGE D EXECUTED — FORGERY/IMMUTABILITY (exit 0, all 12 attacks denied, persistent state ground-truthed UNCHANGED after each).** Auth channel: forged INSERT → 42501 RLS, count unchanged · UPDATE row 1 → 42501 privilege-revoked (RF-3), actor_role unchanged · DELETE row 1 → 42501, count unchanged · **D-2 direct `audit_write` RPC → PGRST202 "Could not find the function public.audit_write without parameters in the schema cache"** — refused at the PostgREST function-resolution layer (NOT 42501 ACL denial, NOT P0001 trigger-context rejection; the implicit `authenticated=X` ACL entry is unreachable via the API channel). Svc channel: UPDATE actor_role/old_data/new_data/context (4×) → P0001 "audit_logs is append-only", state UNCHANGED each · DELETE row 1 → P0001, count 640 unchanged · owner-channel TRUNCATE → P0001 (R-6a statement guard), count unchanged. Final ground truth: target row byte-for-byte original. | Can ANY forbidden channel mutate persistent audit history? **NO — zero persistent mutations across auth+svc+owner.** D-2 observed shape recorded without stretching AC-114's letter (revoked public+anon = literally satisfied; trigger function not invocable via API) | **Durable evidence** | none persistent (all mutations denied below/at RLS; TRUNCATE rolled back by guard before execution) | n/a |
| 9 | 2026-09-15 16:43 (Cody) | `QA/evidence/QA_E_probes_2026-09-15T0843.log` | **STAGE E EXECUTED — TENANT BOUNDARY MATRIX (exit 0, 8/8 refused + control green).** member-A→B1 kpis+rows · admin-A→B1 kpis+rows · admin-B→A1 kpis+rows · multi→A2 kpis+rows — **every one: P0001 "not a member of business", no PHI returned, +0 read_page (no misleading audit row)**. Control: member-A→owedbook_kpis(A1) succeeds with the expected KPI result AND exactly +1 read_page. multiAdmin (A1+B1) correctly refused on A2 (ownerA-only) — D-5 asymmetry handled live. | Any cross-tenant PHI leakage? **NONE.** Does every refusal fire pre-read (no audit side effect)? **YES.** | **Durable evidence** | none (all refused; one legitimate audited control call) | n/a |
| 10 | 2026-09-15 16:47 (Cody) | `QA/evidence/QA_F_probes_2026-09-15T0847.log` | **STAGE F EXECUTED — SERVICE-ROLE OBSERVATION (exit 0).** `svc.rpc owedbook_kpis(A1)`: **DENIED — P0001 "not a member of business"**, +0 read_page. Cross-tenant `owedbook_kpis(B1)` via svc: same P0001. **This DISPROVES recon §5 item 3's static expectation** (expected allowed + NULL-actor read_page row): with service_role, `auth.uid()` is NULL → `my_business_ids()` returns empty → membership fence raises before any read. No wrapper read is possible by svc at all → no unaudited svc read channel exists. Observation for SOL (no frozen-contract literal on svc wrapper EXECUTE). | Is there an unaudited service-role wrapper read channel? **NO** | **Durable evidence** | none (denied) | n/a |
| 11 | 2026-09-15 16:47 (Cody) | `QA/evidence/QA_G_probes_2026-09-15T0847.log` | **STAGE G EXECUTED — ATTRIBUTION (exit 0).** admin-A audited write (user_data insert): trail row actor_user_id=admin-A, actor_role=authenticated, business_id=A1, action=insert, row_id=pk. Wrapper read pre-refresh: ok. **Real supabase-js `refreshSession()`: succeeded** (probe supports it — not an untested item). Wrapper read post-refresh: succeeded; **both read_page rows carry the SAME identity (admin-A, authenticated) across the refresh** — no attribution loss on token rotation. QA write row tidied by admin-A delete (itself an audited, admin-legal mutation). | Does attribution persist for authenticated writes and wrapper reads, incl. across token refresh? **YES** | **Durable evidence** | one user_data row inserted then deleted by admin-A (both audited) — expected residue: those trail rows remain, which is the mechanism working as designed | n/a |

| 12 | 2026-09-15 17:01 (Cody, replacement session after CLI bounce) | `QA/rls-prove/ABORTED-X5-matrix_2026-09-15T0855.log` · `QA/rls-prove/ABORTED-X5_ac8_fresh_2026-09-15T0855.log` | **SESSION RECOVERY + ABORTED-ATTEMPT TRIAGE.** Read order honoured (RECOVERY → session log → this journal → recon). Specimen re-pinned unchanged (`c45949e`). Found two partial files from the ~16:50 pre-bounce Stage H attempt in Engineering's BIM-002 evidence folder: matrix (320 cells GREEN) + ac8_fresh (GREEN) but **no** `X5_prove`/scoping/attacks/revocation → runner was interrupted after the matrix stage, never reached a verdict. Relocated as `ABORTED-*` (audit trail only, NOT proof evidence). Engineering folder verified back at committed state. SOL's C–I release text not on disk and not re-pasted → final report's 12-section layout reconstructed and flagged. | Was the pre-bounce Stage H a valid run? **No — interrupted, non-proof.** | Bookkeeping | none | n/a |
| 13 | 2026-09-15 17:02–17:04 (Cody) | `QA/rls-prove/X5_prove_2026-09-15T0902.log` (+ `.normalised.log`) · `X5-matrix_…T0903.log` · `X5_ac8_fresh_…T0903.log` · `X4_scoping_…T0903.log` · `X4_attacks_…T0904.log` · `X4_revocation_…T0904.log` (7 files, relocated from Engineering's BIM-002 folder; folder back at committed state) | **STAGE H EXECUTED — `npm run rls:prove` exit 0, ISOLATION PROVEN** after Stages C–G's destructive activity. 8/8 stages: chain 47/47 · AC8 from scratch GREEN · 19 policies · **320 cells / 0 mismatches** · SCOPING EXACT · **28 attacks / 0 breaches / 0 ground-truth mismatches** · R-C revocation + re-grant immediate on a byte-identical token. A8.3 staffA→audit_logs DENY while svc sees 634 rows. Twin vs Stage A normalised: one runtime line (`purged 5` vs `4 pre-existing auth.users`), every proof line identical. | Any BIM-002 tenant-security regression after the C–G attacks? **NONE.** | **Durable evidence** | SCRATCH wiped + replayed (rls:prove stage 1, authorized). Seed maps regenerated (by design). REPLICA untouched. | n/a — SCRATCH disposable |
| 14 | 2026-09-15 17:05–17:06 (Cody) | `QA/evidence/QA_I_triad_2026-09-15T1705.log` · `QA/evidence/S3_audit_prove_2026-09-15T0905.log` (+ `.normalised.log`) · `S3_audit_session_2026-09-15T0906.log` · `…0906.produced.json` (4 files relocated from Engineering's BIM-003 folder; folder back at committed state) | **STAGE I EXECUTED — triad + final audit:prove, ALL GREEN.** Exact S3 commands: `npm run build` exit 0 **22 routes** (route tree identical to S3) · `npx tsc --noEmit` exit 0 · `npm test` exit 0 **28 suites / 128 tests / 0 fail**. Final `npm run audit:prove` exit 0 **TRAIL PROVEN**: 6 tamper probes green, write trail + attribution, R-8, six wrapper reads → six read_page rows, cross-tenant deny, AC-113, scoping admin-A 421 / member-A 0 / admin-B 206 / multi 424 / anon 0, **golden produced 10 / golden 10 / 0 diffs → TRAIL EXACT**. Twins: final normalised vs Stage A (T0642) and vs Engineering committed (T0738) — **timestamp line only**, both. Golden SHA `c30bfcdd…cd4f` unchanged, `git diff` empty. Product tree: zero tracked changes beyond RECOVERY + seed maps. | Is the candidate green after every destructive activity of the campaign? **YES.** Does the triad hold? **YES, 22/0/28-128-0.** | **Durable evidence** | SCRATCH wiped + replayed (audit:prove stage 1, authorized); now holds the final golden-session seeded state. Seed maps regenerated (by design). REPLICA untouched. Golden untouched. | n/a |

| 15 | 2026-09-15 17:32 (Fable, SOL + Director cleanup release, J-19) | `QA/evidence/` (−20) · `QA/rls-prove/` (−2) · `scripts/rls-harness/seed-map.json` + `audit-seed-map.json` (restored) · `RULINGS_ADDENDUM.md` (+CF-10, +CF-8 corroboration) · `RETROSPECTIVE.md` (+1 ledger row) · `BIM003_ACCEPTANCE_SPEC.md` (+E-8, erratum lane only) · `CHANGELOG.md` (+1) | **QA CLEANUP EXECUTED after GATE Q = PASS (SOL, certified `c45949e`).** Pre-verified all five Stage-B `.produced.json` cmp-identical to Stage A's, then deleted the 20 duplicate `S3_audit_prove/session_*T0714…T0726` files and the 2 `ABORTED-*T0855` partials. Seed maps restored via read-only `git show c45949e:<path> >` (no git mutation); `git status scripts/` empty. Durable package: README + recon + journal + helpers ×3 + evidence ×26 + rls-prove ×14 (disposition headline said 27 — arithmetic slip; its per-item inventory sums to 26, nothing missing). No `QA/RESPONSE/` copies. **RECOVERY.md and `agent_docs/SESSIONS/**` untouched — Director-protected.** No DB, no proof rerun, no build/tsc/test, no git. | Is the QA lane reduced to exactly the durable certification package with zero harness/product drift? **YES** | Bookkeeping | none | n/a — maps restored to certified bytes |

**CAMPAIGN POSITION (updated 2026-09-15 17:32):** **GATE Q = PASS (SOL, certified `c45949e`). QA CLEANUP COMPLETE (row 15) — awaiting SOL merge-ready confirmation.** Stages A–I all executed, all green, zero STOP conditions fired, zero implementation defects. Consolidated report: `agent_docs/RESPONSES/response_2026-09-15_170730_cody-stages-c-i-preq-report.md`. **PRE-Q C–I COMPLETE — AWAITING SOL ADJUDICATION.** Open for SOL: D-1 ruling (contract-spec), D-3 erratum row, seed-map disposition (QA Cleanup), `ABORTED-*` partials keep/delete, evidence-path hard-coding (CF-8 candidate). SCRATCH = final audit:prove seeded state. REPLICA untouched. Golden untouched. No product file modified. No git mutation by the agent.

_(Row-1…3 footer, historical: "No destructive command executed this stage. SCRATCH untouched by QA. No product file touched." — superseded by rows 4–14 above.)_